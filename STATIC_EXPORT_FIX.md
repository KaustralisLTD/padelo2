# ⚠️ Проблема со статическим экспортом

## Проблема:

Next.js с next-intl использует `headers` для определения локали, что не работает в статическом экспорте.

## ✅ Решение:

Несмотря на ошибки, **папка `out/` все равно создается** с теми страницами, которые удалось сгенерировать.

### Вариант 1: Использовать то, что есть (быстро)

1. Проверьте, создалась ли папка `out/`:
```bash
ls -la out/
```

2. Если папка есть, загрузите её содержимое на FTP - основные страницы должны работать.

3. Страницы, которые не сгенерировались, будут рендериться на клиенте (в браузере).

### Вариант 2: Исправить для полного статического экспорта (требует изменений)

Нужно добавить `setRequestLocale` в каждую страницу. Это требует времени.

## 🚀 Что делать сейчас:

### 1. Проверьте папку out/:

```bash
npm run build
ls -la out/
```

### 2. Если папка создалась, загрузите на FTP:

**Через FileZilla:**
- Подключитесь к FTP
- Загрузите **ВСЁ содержимое** папки `out/` в корневую директорию сайта (обычно `public_html/`)

### 3. Проверьте сайт:

Откройте ваш домен - сайт должен работать, даже если некоторые страницы рендерятся динамически на клиенте.

## 📝 Примечание:

Для полного статического экспорта с next-intl нужно:
1. Добавить `setRequestLocale` в layout и страницы
2. Или использовать другой подход к локализации

Но текущая версия **должна работать** - страницы будут рендериться на клиенте, что нормально для статического сайта.

