# 🔐 Доступы администратора PadelO₂

## Дефолтный Super Admin

**Email:** `admin@padelo2.com`  
**Пароль:** `admin123`  
**Роль:** `superadmin`

---

## ⚠️ Важная информация

### Если БД не настроена (in-memory режим):
- Админ создается автоматически при первом запросе к `/api/auth/login`
- **Данные теряются при перезапуске сервера!**
- После каждого перезапуска нужно снова делать первый запрос к API

### Если БД настроена:
- Админ создается автоматически при первом запросе к API (если таблица users пустая)
- Данные сохраняются в MySQL
- Админ не теряется при перезапуске

---

## 🔧 Создание админа вручную

### Вариант 1: Через скрипт (если БД настроена)

```bash
npx tsx scripts/create-admin.ts
```

### Вариант 2: Через SQL (если БД настроена)

```sql
INSERT INTO users (id, email, password_hash, first_name, last_name, role) 
VALUES (
  UUID(),
  'admin@padelo2.com',
  '$2b$10$...', -- bcrypt hash для 'admin123'
  'Super',
  'Admin',
  'superadmin'
);
```

Для генерации bcrypt hash:
```bash
node -e "const bcrypt = require('bcrypt'); bcrypt.hash('admin123', 10).then(h => console.log(h))"
```

### Вариант 3: Через админ-панель (если уже есть админ)

1. Войдите как существующий админ
2. Перейдите в `/admin/users`
3. Создайте нового пользователя с ролью `superadmin`

---

## 🐛 Решение проблем

### "Invalid email or password"

**Проблема:** Админ не создан или данные потеряны

**Решение:**
1. Проверьте, настроена ли БД (переменные DATABASE_* в .env.local)
2. Если БД не настроена:
   - Перезапустите dev-сервер
   - Сделайте запрос к `/api/auth/login` (попробуйте войти)
   - Админ создастся автоматически
3. Если БД настроена:
   - Запустите скрипт: `npx tsx scripts/create-admin.ts`
   - Или создайте через SQL

### Проверка существования админа

**Если БД настроена:**
```sql
SELECT email, role, created_at FROM users WHERE email = 'admin@padelo2.com';
```

**Если БД не настроена:**
- Проверьте логи сервера при попытке входа
- Должно быть сообщение: "✅ In-memory admin created"

---

## 📝 Система ролей

### Superadmin
- Полный доступ ко всем функциям
- Управление пользователями
- Управление турнирами
- Управление персоналом

### Staff
- Управление турнирами
- Управление регистрациями
- Редактирование игроков

### Participant
- Доступ к личному dashboard
- Просмотр своей регистрации

---

## 🔒 Безопасность

⚠️ **ВАЖНО для production:**
1. Смените дефолтный пароль `admin123` сразу после первого входа
2. Создайте дополнительные учетные записи
3. Удалите или переименуйте дефолтного админа
4. Используйте сильные пароли

---

## 📍 Где находится код

- Инициализация: `lib/users.ts` → `initializeDefaultAdmin()`
- Автоматический вызов: `app/api/auth/login/route.ts`
- Скрипт создания: `scripts/create-admin.ts`

