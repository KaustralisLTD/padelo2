# Шаги для деплоя на Vercel

## ✅ Что уже сделано:
- ✅ Код загружен в GitHub: `https://github.com/KaustralisLTD/padelo2.git`
- ✅ Репозиторий готов к деплою

## 📋 Следующие шаги в Vercel Dashboard:

### 1. Добавить новый проект
- Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
- Нажмите **"Add New..."** → **"Project"**

### 2. Выбрать репозиторий
- Найдите и выберите репозиторий: **`KaustralisLTD/padelo2`**
- Если репозиторий не виден, нажмите **"Adjust GitHub App Permissions"** и дайте доступ к организации `KaustralisLTD`

### 3. Настройки проекта
Vercel автоматически определит Next.js, но проверьте:

- **Framework Preset:** `Next.js` (автоматически)
- **Root Directory:** `./` (по умолчанию)
- **Build Command:** `npm run build` (по умолчанию)
- **Output Directory:** `.next` (по умолчанию)
- **Install Command:** `npm install` (по умолчанию)

### 4. Environment Variables (если нужны)
Пока можно пропустить — переменные окружения не требуются для базового деплоя.

### 5. Деплой
- Нажмите **"Deploy"**
- Дождитесь завершения сборки (обычно 2-3 минуты)

### 6. Результат
После успешного деплоя вы получите:
- **Production URL:** `https://padelo2.vercel.app` (или похожий)
- **Preview URLs** для каждого коммита

## 🌐 Настройка домена (опционально)
Если хотите использовать свой домен `padelo2.com`:
1. В настройках проекта → **"Domains"**
2. Добавьте домен `padelo2.com`
3. Следуйте инструкциям по настройке DNS

## 🔄 Автоматические деплои
После подключения каждый `git push` в `main` будет автоматически деплоить изменения!

---

**Готово!** После деплоя ваш сайт будет доступен по адресу, который предоставит Vercel.

