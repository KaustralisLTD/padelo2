# План начала работ - PadelO₂

> **Дата создания**: 2025-11-14  
> **Дата обновления**: 2025-01-21 (обновлено: исправление определения языка, отключение Apple OAuth, исправления UI/UX, email шаблоны, TikTok ссылка, SEO исправления, автоматическая верификация SuperAdmin, локализация размеров футболок, улучшение навигации, сжатие изображений)  
> **Приоритет**: Критические проблемы → UX улучшения → Новые функции

## 🚀 Быстрые команды для работы

### Коммит и отправка изменений в Vercel:
```bash
# 1. Проверить статус изменений
git status

# 2. Добавить все изменения
git add -A

# 3. Создать коммит с описанием
git commit -m "Описание изменений"

# 4. Отправить в репозиторий (автоматически деплоится на Vercel)
git push origin main
```

### Проверка перед коммитом:
```bash
# Проверить линтер
npm run lint

# Проверить типы TypeScript
npm run type-check  # если есть такая команда

# Проверить сборку
npm run build
```

---

## 🎯 Фаза 1: Критические исправления (1-2 дня)

### Задача 1.1: Исправление потери ID пар при перемещении ⚠️ КРИТИЧНО
**Проблема**: При перемещении пар между группами теряется уникальный ID, появляются дубликаты (Pair 7, Pair 8 в одной категории)

**Решение**:
1. Использовать `id` из `tournament_group_pairs` как постоянный идентификатор
2. Сохранять оригинальный `pair_number` при перемещении (или использовать уникальный ID)
3. Обновить визуальное отображение - показывать уникальный ID пары

**Файлы для изменения**:
- `app/api/tournament/[id]/pair/move/route.ts` - исправить логику перемещения
- `components/TournamentBracket.tsx` - обновить отображение пар
- `app/api/tournament/[id]/bracket/route.ts` - обновить API для получения пар

**Шаги**:
```typescript
// Вместо пересчета pair_number, использовать уникальный ID
// Вариант 1: Сохранять оригинальный pair_number
// Вариант 2: Использовать id из tournament_group_pairs как отображаемый номер
// Вариант 3: Добавить поле unique_pair_id в таблицу
```

**Оценка времени**: 2-3 часа

---

### Задача 1.2: Диагностика и оптимизация производительности БД 🚀
**Проблема**: Очень медленная работа сайта, долгие загрузки при входе

**Шаги диагностики**:
1. Проверить активные соединения к БД
2. Найти медленные запросы
3. Добавить индексы на часто используемые поля
4. Проверить N+1 проблемы в запросах

**SQL для диагностики**:
```sql
-- Проверить активные соединения
SHOW PROCESSLIST;

-- Проверить медленные запросы (если включен slow query log)
SHOW VARIABLES LIKE 'slow_query_log';

-- Проверить индексы
SHOW INDEX FROM users;
SHOW INDEX FROM sessions;
SHOW INDEX FROM tournament_registrations;
```

**Индексы для добавления**:
```sql
-- Если их еще нет
CREATE INDEX idx_sessions_token ON sessions(token);
CREATE INDEX idx_sessions_user_id ON sessions(user_id);
CREATE INDEX idx_tournament_registrations_tournament_id ON tournament_registrations(tournament_id);
CREATE INDEX idx_tournament_registrations_user_id ON tournament_registrations(user_id);
CREATE INDEX idx_tournament_groups_tournament_category ON tournament_groups(tournament_id, category);
```

**Файлы для проверки**:
- `lib/db.ts` - проверить настройки пула
- `app/api/auth/login/route.ts` - оптимизировать запросы при входе
- `lib/users.ts` - оптимизировать запросы пользователей
- Все файлы в `app/api/` - проверить на N+1 проблемы

**Оценка времени**: 4-6 часов

---

### Задача 1.3: Проверка и исправление проблемы с сессиями админа
**Проблема**: Админ выходит из системы, не может войти повторно

**Шаги**:
1. Проверить логику создания сессий
2. Проверить срок действия токенов
3. Добавить логирование при проверке сессий
4. Проверить middleware

**Файлы для проверки**:
- `lib/users.ts` - функции `createSession`, `getSession`, `deleteSession`
- `app/api/auth/login/route.ts` - логика входа
- `middleware.ts` - проверка авторизации

**SQL для диагностики**:
```sql
-- Проверить сессии админа
SELECT * FROM sessions 
WHERE user_id = (SELECT id FROM users WHERE email = 'admin@padelo2.com')
ORDER BY created_at DESC;

-- Проверить истекшие сессии
SELECT * FROM sessions WHERE expires_at < NOW();
```

**Оценка времени**: 2-3 часа

---

## 🎨 Фаза 2: Быстрые улучшения UX (1 день)

### Задача 2.1: Login → Account в меню ✅ ВЫПОЛНЕНО
**Статус**: Переводы добавлены для всех языков

---

### Задача 2.2: Dashboard в основном меню ✅ ВЫПОЛНЕНО
**Статус**: Dashboard добавлен в меню для авторизованных пользователей

---

### Задача 2.3: Кнопки копирования для полей участников ✅ ВЫПОЛНЕНО
**Реализовано**: 
- Кнопки копирования для User ID, имени, email, телефона, имени партнера
- Иконки появляются при наведении
- Уведомление "Скопировано!" при успешном копировании
- Локализация для всех 14 языков

**Файлы**:
- `app/[locale]/admin/tournaments/[id]/participants/page.tsx`
- `messages/*.json` (добавлены ключи `copy`, `copied`)

---

### Задача 2.4: Фильтры и поиск в таблице участников ✅ ВЫПОЛНЕНО
**Реализовано**:
- Поиск по имени участника и партнера (включая партнеров по категориям)
- Фильтр по наличию партнера (Все / С партнером / Без партнера)
- Фильтр по статусу оплаты (Все / Ожидает / Оплачено / Возврат)
- Фильтр по категориям (множественный выбор)
- Кнопка "Сбросить фильтры"

**Файлы**:
- `app/[locale]/admin/tournaments/[id]/participants/page.tsx`
- `messages/*.json` (добавлены ключи для фильтров)

---

### Задача 2.5: Проверка на дубликаты участников ✅ ВЫПОЛНЕНО
**Реализовано**:
- Автоматическое обнаружение дубликатов по:
  - Имя + Фамилия + Email
  - Телефон
  - Email
- Подсветка дубликатов красным фоном (`bg-red-500/10 border-red-500/50`)

**Файлы**:
- `app/[locale]/admin/tournaments/[id]/participants/page.tsx`

---

### Задача 2.6: Автоматическое добавление партнера как участника ✅ ВЫПОЛНЕНО
**Реализовано**:
- При сохранении участника с партнером система:
  - Проверяет наличие партнера в базе по email, телефону или имени
  - Если партнер не найден, создает нового пользователя с `user_id`
  - Создает регистрацию для партнера в этом турнире
  - Работает для обычного партнера и партнеров по категориям

**Файлы**:
- `app/api/tournament/[id]/registrations/[registrationId]/route.ts`
- Добавлены функции `getOrCreateUserByPartnerInfo` и `ensurePartnerRegistration`

---

### Задача 2.7: Проверка одинакового партнера в разных категориях ✅ ВЫПОЛНЕНО
**Реализовано**:
- Если у участника выбраны несколько категорий и в разных категориях указан один и тот же партнер:
  - Подсветка проблемных категорий красным фоном
  - Предупреждающий значок ⚠
  - Подсказка при наведении: "Один партнер не может быть в разных категориях"

**Файлы**:
- `app/[locale]/admin/tournaments/[id]/participants/page.tsx`
- `messages/*.json` (добавлен ключ `samePartnerError`)

---

## 🔧 Фаза 3: Улучшения функционала (2-3 дня)

### Задача 3.1: Улучшение редактирования пар в Tournament Bracket
**Требуется**: 
- Прямое редактирование имен игроков
- Возможность добавить нового игрока (не из БД)
- Кнопки редактирования рядом с именами
- Уведомления через Email и WhatsApp

**Шаги**:
1. Обновить модальное окно редактирования пары
2. Добавить поля ввода для имен (не только выбор)
3. Добавить чекбокс "Уведомить участников"
4. Создать `lib/whatsapp.ts` для отправки WhatsApp
5. Обновить API для сохранения изменений

**Файлы для изменения**:
- `components/TournamentBracket.tsx` - модальное окно
- `app/api/tournament/[id]/pair/route.ts` - API
- `lib/email.ts` - отправка email
- `lib/whatsapp.ts` - создать новый файл

**Оценка времени**: 6-8 часов

---

### Задача 3.2: Детальная информация о турнире (адрес, Google Maps, расписание)
**Требуется**: 
- Поле адреса с Google Maps
- Расписание событий (лотерея, автограф-сессии)

**Шаги**:
1. Обновить схему БД - добавить поля в `tournaments`:
   - `location_address TEXT`
   - `location_coordinates JSON`
   - `event_schedule JSON`
2. Обновить форму редактирования турнира
3. Добавить Google Maps API интеграцию
4. Обновить страницу деталей турнира

**Файлы для изменения**:
- `lib/db.ts` - обновить схему
- `components/pages/AdminTournamentsContent.tsx` - форма
- `components/TournamentDetails.tsx` - отображение

**Зависимости**:
- Google Maps API ключ
- `@react-google-maps/api` или `google-maps-react`

**Оценка времени**: 8-10 часов

---

## ✅ Выполненные задачи (последние обновления)

### Управление участниками турнира:
- ✅ Кнопки копирования для всех полей (User ID, имя, email, телефон, партнер)
- ✅ Фильтры и поиск (по имени, партнеру, статусу оплаты, категориям)
- ✅ Проверка на дубликаты с подсветкой
- ✅ Автоматическое создание регистрации для партнера
- ✅ Проверка одинакового партнера в разных категориях
- ✅ Локализация всех новых элементов на 14 языков

### OAuth авторизация:
- ✅ Google OAuth (логин и регистрация)
- ✅ Apple OAuth (логин и регистрация)
- ✅ Интеграция с существующей системой пользователей
- ✅ Автоматическое создание `user_id` для регистраций

### Демо-турниры:
- ✅ Статус "Demo" для турниров
- ✅ Создание демо-участников с распределением по категориям
- ✅ Реалистичные случайные имена для демо-участников
- ✅ Автоматическая генерация турнирной сетки для демо-турниров
- ✅ Удаление демо-участников

### Настройки регистрации:
- ✅ Гибкие настройки формы регистрации (T-shirt, кастомные поля, обязательность партнера)
- ✅ Кастомные категории для каждого турнира
- ✅ Управление категориями (добавление, переименование, удаление)

### Управление участниками:
- ✅ Отдельная страница `/admin/tournaments/[id]/participants`
- ✅ Редактирование данных участника
- ✅ Изменение статуса оплаты
- ✅ Управление категориями участника (множественный выбор)
- ✅ Партнеры для разных категорий
- ✅ Колонки "Порядковый номер" и "User ID"

### Управление турнирами:
- ✅ Редактирование статуса турнира прямо в таблице `/admin/tournaments`
- ✅ Клик на статус открывает выпадающий список для быстрого изменения
- ✅ Автоматическое сохранение изменений в БД
- ✅ Локализация сообщений об успешном обновлении статуса

### Страницы участника:
- ✅ Страница `/participant/wallet` - просмотр баланса и транзакций
- ✅ Страница `/participant/schedule` - календарь кортов с временными слотами
- ✅ Страница `/participant/results` - история матчей и статистика
- ✅ Страница `/dashboard` - главная панель участника с равномерным размещением карточек
- ✅ Исправлена ошибка `toFixed` на странице wallet
- ✅ Улучшены границы календаря (более мягкие, не резкие)

### UI/UX улучшения (2025-01-21):
- ✅ Исправлены цвета успешных сообщений (заменен яркий зеленый на мягкий primary цвет)
- ✅ Улучшены границы календаря на `/participant/schedule` (более темные, не резкие для глаз)
- ✅ Исправлено размещение карточек на `/dashboard` (равномерное распределение в 3 колонки)
- ✅ Добавлены переводы для всех ошибок и сообщений на всех 14 языках
- ✅ Исправлены переводы для страницы регистрации на турнир (русский язык)
- ✅ Переключатель языков в шапке и меню обновлен: компактная кнопка с кодом языка и флагом, единый стиль для всех 14 локалей (для `ru` используется украинский флаг по требованию)
- ✅ Логика выбора og-изображения и schema.org логотипа переведена на `logo-header.png`, настроены абсолютные URL с доменом `https://www.padelo2.com`, чтобы в мессенджерах подтягивался правильный логотип
- ✅ Ссылки вида `/tournaments#register-{id}` теперь авто-скроллят страницу точно к заголовку регистрации с учетом хедера (отступ 100px), что позволяет делиться прямыми ссылками на форму выбранного турнира
- ✅ Блок "Інструкції з завантаження фото" на странице регистрации перемещен сразу под загрузку фото участника, чтобы пользователи сразу видели требования к фото
- ✅ Переключатель темы на мобильных устройствах получил фиксированный компактный вид без перетягивания и крестика: по тапу сразу меняет тему, не перекрывает контент
- ✅ Исправлены переводы для WhatsApp сообщений (поля "Name" и "User ID")

### Оптимизация кода и исправление предупреждений (2025-01-21):
- ✅ Заменены все `<img>` теги на `<Image />` из `next/image` для оптимизации производительности
- ✅ Исправлены все предупреждения React Hooks (useEffect dependencies)
- ✅ Исправлено использование Google Analytics (заменено на `next/script`)
- ✅ Добавлены импорты `Image` во все необходимые компоненты
- ✅ Сборка проходит без предупреждений и ошибок

### Email система:
- ✅ Современные стильные email шаблоны для подтверждения регистрации
- ✅ Поддержка 14 языков в email шаблонах
- ✅ Интеграция с Resend для отправки писем
- ✅ Адаптивный дизайн email шаблонов
- ✅ Документация по редактированию шаблонов (см. секцию "Email Шаблоны")

### UI/UX улучшения:
- ✅ Исправлены цвета успешных сообщений (заменен яркий зеленый на мягкий primary цвет)
- ✅ Улучшены границы календаря на `/participant/schedule` (более темные, не резкие)
- ✅ Исправлено размещение карточек на `/dashboard` (равномерное распределение в 3 колонки)
- ✅ Добавлены переводы для всех ошибок и сообщений на всех 14 языках
- ✅ Исправлены переводы для страницы регистрации на турнир (русский язык)

### WhatsApp интеграция:
- ✅ Автоматическое добавление имени пользователя и User ID в сообщение
- ✅ Локализация полей "Name" и "User ID" для всех языков

### Социальные сети и SEO (2025-01-21):
- ✅ Добавлена ссылка на TikTok в футер сайта (`components/Footer.tsx`)
- ✅ Исправлены проблемы Google Search Console для `/zh/machines`:
  - Добавлено обязательное поле `price` в offers (установлено значение '0' для PreOrder)
  - Исправлена длина поля `addressCountry` в `shippingDetails.shippingDestination` (используется массив ISO кодов стран)
  - Исправлена длина поля `applicableCountry` в `hasMerchantReturnPolicy` (используется массив ISO кодов стран)
- ✅ Файлы: `lib/schema.ts`, `app/[locale]/machines/page.tsx`

### Авторизация и безопасность (2025-01-21):
- ✅ Реализована автоматическая верификация email для пользователей с ролью `superadmin` при входе
- ✅ SuperAdmin может входить без подтверждения email (проверка пропускается в `app/api/auth/login/route.ts`)
- ✅ Решена проблема с выходом из системы для админа из-за неподтвержденного email

### Локализация и переводы (2025-01-21):
- ✅ Локализованы размеры футболок (ANCHO/LARGO) для всех 14 языков:
  - Добавлены ключи `tshirtWidth` и `tshirtLength` во все файлы локализации
  - Исправлены переводы для всех языков (en, ru, ua, es, fr, de, it, ca, nl, da, sv, no, ar, zh)
- ✅ Исправлены переводы для полей категории KIDS на странице регистрации:
  - `childInfo`, `childFirstName`, `childLastName`, `childPhoto` переведены на все 14 языков
- ✅ Исправлена проблема с украинскими переводами на странице турниров:
  - Блоки "Tournament Details" и "Event Schedule" теперь корректно отображаются на украинском языке
  - Проверена корректность переводов в БД для всех языков

### UI/UX улучшения навигации (2025-01-21):
- ✅ Улучшен UX/UI главного меню навигации:
  - Реализовано адаптивное меню с автоматическим переходом в гамбургер-меню на мобильных устройствах
  - Меню автоматически сворачивается, если элементы не помещаются на экране
  - Добавлены плавные анимации открытия/закрытия меню (framer-motion)
  - Улучшена визуальная обратная связь при взаимодействии
  - Файл: `components/Header.tsx`

### Оптимизация загрузки изображений (2025-01-21):
- ✅ Добавлено автоматическое сжатие изображений перед загрузкой:
  - Создана утилита `lib/image-compression.ts` для сжатия изображений на клиенте
  - Изображения автоматически сжимаются до максимум 500KB перед отправкой на сервер
  - Максимальный размер изображения: 1200x1200px с сохранением пропорций
  - Качество автоматически снижается при необходимости для достижения целевого размера
  - Применено сжатие для всех фото: пользователя, партнера, ребенка, категорий
  - Обновлены компоненты `TournamentRegistrationForm.tsx` и `DashboardContent.tsx`
  - Решена проблема с ошибкой 413 (Content Too Large)
  - Удалена инструкция о формате имени файла из загрузки фото (фото теперь привязывается к user_id автоматически)

---

## 📋 Порядок выполнения (рекомендуемый)

### День 1 (Критические исправления):
1. ⚠️ **Задача 1.1** - Исправление потери ID пар (2-3 часа) - **ТРЕБУЕТ ВНИМАНИЯ**
2. ⚠️ **Задача 1.2** - Диагностика производительности БД (4-6 часов) - **ТРЕБУЕТ ВНИМАНИЯ**
   - Проверить соединения
   - Добавить индексы
   - Оптимизировать запросы

### День 2 (Продолжение критических + быстрые улучшения):
3. ⚠️ **Задача 1.3** - Исправление сессий админа (2-3 часа) - **ТРЕБУЕТ ВНИМАНИЯ**
4. ✅ **Задача 2.2** - Dashboard в меню (30 минут) - **ВЫПОЛНЕНО**
5. ✅ **Задача 2.3-2.7** - Управление участниками - **ВЫПОЛНЕНО**

### День 3 (Улучшения функционала):
6. ⚠️ **Задача 3.1** - Улучшение редактирования пар (6-8 часов) - **В ПРОЦЕССЕ**
7. ⚠️ **Задача 3.2** - Детальная информация о турнире (8-10 часов) - **В ПРОЦЕССЕ**

---

## 🚀 Быстрый старт (первые 2 часа)

### Шаг 1: Исправить потерю ID пар (КРИТИЧНО)
```bash
# 1. Открыть файл
code app/api/tournament/[id]/pair/move/route.ts

# 2. Изменить логику:
# Вместо пересчета pair_number, использовать уникальный ID пары
# Или сохранять оригинальный pair_number при перемещении
```

### Шаг 2: Добавить индексы в БД
```sql
-- Выполнить в MySQL
CREATE INDEX IF NOT EXISTS idx_sessions_token ON sessions(token);
CREATE INDEX IF NOT EXISTS idx_sessions_user_id ON sessions(user_id);
CREATE INDEX IF NOT EXISTS idx_tournament_registrations_tournament_id ON tournament_registrations(tournament_id);
```

### Шаг 3: Проверить производительность
```bash
# Запустить проект
npm run dev

# Открыть в браузере
# Проверить Network tab в DevTools
# Замерить время загрузки при входе
```

---

## 📊 Метрики успеха

### После Фазы 1 (критические исправления):
- ✅ Нет дубликатов ID пар при перемещении
- ✅ Время входа < 1 секунды
- ✅ Админ может войти и оставаться в системе
- ✅ Нет ошибок с соединениями к БД

### После Фазы 2 (UX улучшения):
- ✅ Dashboard виден в меню для авторизованных
- ✅ Все переводы работают корректно

### После Фазы 3 (улучшения функционала):
- ✅ Можно редактировать имена игроков напрямую
- ✅ Уведомления отправляются через Email и WhatsApp
- ✅ Адрес турнира отображается на карте
- ✅ Расписание событий редактируется и отображается

---

## 🔍 Чек-лист перед началом работы

- [ ] Проект запущен (`npm run dev`)
- [ ] База данных доступна
- [ ] Есть доступ к админ-панели (`admin@padelo2.com`)
- [ ] Создана резервная копия БД (на всякий случай)
- [ ] Git репозиторий в чистом состоянии (или создан коммит)
- [ ] Проверены последние изменения в репозитории (`git pull origin main`)

---

## 📝 Инструкция для перехода в новый чат

### Контекст для нового чата:

**Последние выполненные задачи:**
1. ✅ Фильтры и поиск в таблице участников (`/admin/tournaments/[id]/participants`)
2. ✅ Кнопки копирования для всех полей (User ID, имя, email, телефон, партнер)
3. ✅ Проверка на дубликаты участников с подсветкой красным
4. ✅ Автоматическое создание регистрации для партнера при сохранении участника
5. ✅ Проверка одинакового партнера в разных категориях с предупреждением
6. ✅ Локализация всех новых элементов на 14 языков

**Критические задачи, требующие внимания:**
1. ⚠️ **Задача 1.1** - Исправление потери ID пар при перемещении между группами
2. ⚠️ **Задача 1.2** - Диагностика и оптимизация производительности БД
3. ⚠️ **Задача 1.3** - Проверка и исправление проблемы с сессиями админа

**Важные файлы:**
- `app/[locale]/admin/tournaments/[id]/participants/page.tsx` - страница управления участниками
- `app/api/tournament/[id]/registrations/[registrationId]/route.ts` - API для обновления регистраций
- `messages/*.json` - файлы локализации (14 языков)

**Команды для работы:**
```bash
# Проверить статус
git status

# Добавить изменения
git add -A

# Создать коммит
git commit -m "Описание изменений"

# Отправить в Vercel (автоматический деплой)
git push origin main
```

**Структура проекта:**
- Next.js 14 с App Router
- TypeScript
- MySQL база данных
- next-intl для локализации (14 языков)
- Tailwind CSS для стилей

**Важные эндпоинты:**
- `/api/tournament/[id]/registrations` - получение участников
- `/api/tournament/[id]/registrations/[registrationId]` - обновление участника
- `/api/tournament/[id]/demo-participants` - создание/удаление демо-участников
- `/api/admin/tournaments` - управление турнирами

**База данных:**
- Таблица `tournament_registrations` - регистрации участников
- Таблица `users` - пользователи системы
- Таблица `tournament_groups` - группы турнира
- Таблица `tournament_group_pairs` - пары в группах
- Таблица `tournament_matches` - матчи

**Окружение:**
- Vercel для деплоя
- GitHub репозиторий: `KaustralisLTD/padelo2`
- Автоматический деплой при push в `main` ветку

---

## 📝 Примечания

1. **Начинать с критических проблем** - они блокируют работу системы
2. **Тестировать после каждого изменения** - не накапливать проблемы
3. **Коммитить часто** - после каждой завершенной задачи
4. **Документировать изменения** - обновлять этот файл
5. **Проверять линтер** - перед коммитом запускать `npm run lint`
6. **Проверять локализацию** - все новые строки должны быть переведены на 14 языков

---

## 🔄 Workflow для работы

### 1. Перед началом работы:
```bash
# Обновить код из репозитория
git pull origin main

# Проверить статус
git status

# Запустить проект
npm run dev
```

### 2. Во время работы:
- Делать изменения в коде
- Тестировать локально
- Проверять линтер: `npm run lint`

### 3. После завершения задачи:
```bash
# Проверить изменения
git diff

# Добавить все изменения
git add -A

# Создать коммит с понятным описанием
git commit -m "Краткое описание: детали изменений"

# Отправить в репозиторий (автоматический деплой на Vercel)
git push origin main
```

### 4. После деплоя:
- Проверить работу на production (Vercel)
- Убедиться, что нет ошибок в логах
- Обновить этот файл с выполненными задачами

---

## 🎯 Приоритеты на ближайшее время

### Высокий приоритет (критические):
1. ⚠️ **Задача 1.1** - Исправление потери ID пар при перемещении
2. ⚠️ **Задача 1.2** - Оптимизация производительности БД
3. ⚠️ **Задача 1.3** - Исправление сессий админа
4. ✅ **Задача 1.4** - Исправление определения языка интерфейса (украинский → русский) - **ВЫПОЛНЕНО**
   - Добавлен `localeDetection: true` в middleware для правильного определения языка из заголовка Accept-Language

### Средний приоритет (улучшения):
1. **Задача 3.1** - Улучшение редактирования пар (прямое редактирование имен, уведомления)
2. **Задача 3.2** - Детальная информация о турнире (адрес, карта, расписание)
3. ⏸️ **Задача 3.3** - Включение Apple OAuth (временно отключен)
   - **Статус**: Кнопка Apple login временно скрыта
   - **Причина**: Ожидаем данные от Apple Developer Center (APPLE_CLIENT_ID, APPLE_TEAM_ID, APPLE_KEY_ID, APPLE_PRIVATE_KEY)
   - **Файлы**: 
     - `components/pages/LoginContent.tsx` - кнопка закомментирована
     - `app/api/auth/oauth/apple/route.ts` - API готов, но требует настройки
     - `app/api/auth/oauth/apple/callback/route.ts` - callback готов
   - **Когда включить**: После получения данных от Apple Developer Center
   - **Google OAuth**: Работает и оставлен активным

### Низкий приоритет (новые функции):
- Дополнительные улучшения UX
- Новые функции по запросу пользователей

---

---

## 📧 Email Шаблоны - Инструкция по редактированию

### Основной файл шаблонов
**Файл**: `lib/email-templates.ts`

Этот файл содержит все email шаблоны для системы. При создании новых шаблонов или изменении существующих, используйте этот файл как основу.

### Структура шаблона

#### 1. Интерфейс данных
```typescript
export interface EmailTemplateData {
  tournamentName: string;
  confirmationUrl: string;
  firstName?: string;
  lastName?: string;
  locale?: string;
}
```

#### 2. Функция шаблона
```typescript
export function getConfirmationEmailTemplate(data: EmailTemplateData): string {
  const { tournamentName, confirmationUrl, firstName, lastName, locale = 'en' } = data;
  // ... логика шаблона
}
```

#### 3. Переводы
Все переводы хранятся в объекте `translations` внутри функции шаблона:
```typescript
const translations: Record<string, Record<string, string>> = {
  en: { /* английские переводы */ },
  ru: { /* русские переводы */ },
  ua: { /* украинские переводы */ },
  // ... остальные языки (es, fr, de, it, ca, nl, da, sv, no, ar, zh)
};
```

### Как добавить новый email шаблон

1. **Создайте интерфейс для данных**:
```typescript
export interface NewEmailTemplateData {
  // ваши поля
  userName: string;
  locale?: string;
}
```

2. **Создайте функцию шаблона**:
```typescript
export function getNewEmailTemplate(data: NewEmailTemplateData): string {
  const { userName, locale = 'en' } = data;
  
  // Добавьте переводы для всех 14 языков
  const translations: Record<string, Record<string, string>> = {
    en: { /* ... */ },
    ru: { /* ... */ },
    // ... все языки
  };
  
  const t = translations[locale] || translations.en;
  
  // Верните HTML шаблон
  return `<!DOCTYPE html>...`;
}
```

3. **Используйте единый стиль дизайна**:
   - Радиальный градиент фона: `background: radial-gradient(circle at 50% 0%, rgba(120, 119, 198, 0.3), rgba(255, 255, 255, 0))`
   - Основной контейнер: `max-width: 600px`, `margin: 0 auto`
   - Цвета: используйте цвета из темы сайта (primary, accent)
   - Шрифты: `font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`

4. **Добавьте переводы для всех 14 языков**:
   - en (English), ru (Русский), ua (Українська), es (Español), fr (Français), de (Deutsch), it (Italiano), ca (Català), nl (Nederlands), da (Dansk), sv (Svenska), no (Norsk), ar (العربية), zh (中文)

### Как использовать шаблон в API

```typescript
import { getConfirmationEmailTemplate } from '@/lib/email-templates';
import { sendEmail } from '@/lib/email';

// В вашем API route
const html = getConfirmationEmailTemplate({
  tournamentName: 'UA PADEL OPEN',
  confirmationUrl: 'https://padelo2.com/en/tournament/confirmation?token=...',
  firstName: 'John',
  lastName: 'Doe',
  locale: 'en',
});

await sendEmail({
  to: 'user@example.com',
  subject: 'Confirm your registration',
  html,
});
```

### Важные моменты при редактировании

1. **Всегда добавляйте переводы для всех 14 языков** - даже если временно используете английский текст
2. **Используйте inline стили** - многие email клиенты не поддерживают внешние CSS
3. **Тестируйте на разных устройствах** - используйте адаптивный дизайн
4. **Проверяйте отображение в разных email клиентах** - Gmail, Outlook, Apple Mail
5. **Сохраняйте единый стиль** - используйте те же цвета, шрифты и структуру, что и в основном шаблоне

### Структура HTML шаблона (рекомендуемая)

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body style="margin: 0; padding: 0; background: radial-gradient(...);">
  <table width="100%" cellpadding="0" cellspacing="0">
    <tr>
      <td align="center" style="padding: 40px 20px;">
        <!-- Основной контейнер -->
        <table width="600" cellpadding="0" cellspacing="0" style="background: white; border-radius: 12px;">
          <!-- Header с логотипом -->
          <!-- Контент -->
          <!-- Footer -->
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
```

### Где используются email шаблоны

- `app/api/tournament/register/route.ts` - отправка подтверждения регистрации
- `lib/email.ts` - функции отправки email (sendEmail, sendContactFormEmail, etc.)

### Скрипты для добавления переводов

Если нужно добавить новые ключи переводов в email шаблоны, используйте скрипт:
```bash
node scripts/add-email-translations.js
```

Создайте скрипт по аналогии с `scripts/add-thankYouFor-translations.js`

---

*План создан: 2025-11-14*  
*Последнее обновление: 2025-01-21*  
*Приоритет: Критические проблемы → UX → Новые функции*

