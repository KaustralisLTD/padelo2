# 📦 Деплой статического сайта на обычный FTP хостинг

## ✅ Настройка завершена!

Проект настроен для статического экспорта, который работает на обычном FTP хостинге **без Node.js**.

## 🚀 Быстрый старт:

### 1. Соберите статический сайт:

```bash
npm run build
```

После сборки будет создана папка `out/` со всеми статическими файлами.

### 2. Загрузите на FTP:

**Через FileZilla:**
- Host: `foldis00.ftp.tools`
- User: `foldis00_padel`
- Pass: `pM6ziY9ozU`

**Загрузите ВСЁ содержимое папки `out/`** в корневую директорию вашего сайта на FTP (обычно `public_html/` или `www/`).

### 3. Готово!

Сайт должен работать сразу после загрузки файлов.

## 📋 Что загружать:

Загрузите **всё содержимое** папки `out/`:
- `index.html` и все HTML файлы
- `_next/` (папка со скриптами и стилями)
- Все файлы из `public/` (изображения, видео, логотипы)

## ⚠️ Важные ограничения статического экспорта:

### ❌ Что НЕ будет работать:

1. **API Routes** - формы не будут отправлять данные на сервер
   - **Решение:** Используйте внешний сервис (Formspree, EmailJS, или mailto: ссылки)

2. **Серверный рендеринг** - все страницы предварительно сгенерированы

3. **Динамические маршруты** - только статические страницы

### ✅ Что работает:

- ✅ Все страницы и языки
- ✅ Навигация
- ✅ Изображения и видео
- ✅ Анимации (Framer Motion)
- ✅ Локализация (next-intl)
- ✅ WhatsApp кнопка
- ✅ Все UI компоненты

## 🔧 Настройка форм (опционально):

Поскольку API routes не работают, можно:

### Вариант 1: Использовать EmailJS (бесплатно)

1. Зарегистрируйтесь на [EmailJS.com](https://www.emailjs.com/)
2. Настройте email сервис
3. Обновите `ContactForm.tsx` для отправки через EmailJS

### Вариант 2: Использовать mailto: ссылки

Формы будут открывать почтовый клиент пользователя.

### Вариант 3: Использовать внешний сервис форм

- Formspree
- Netlify Forms
- Google Forms

## 📁 Структура после сборки:

```
out/
├── index.html          # Главная страница (редирект на /en)
├── en/                 # Английская версия
│   ├── index.html
│   ├── about/
│   ├── academy/
│   └── ...
├── es/                 # Испанская версия
├── ru/                 # Русская версия
├── ...                 # Другие языки
├── _next/              # Скрипты и стили Next.js
└── [файлы из public/]  # Изображения, видео, логотипы
```

## 🔄 Обновление сайта:

При каждом изменении:

1. Внесите изменения в код
2. Выполните: `npm run build`
3. Загрузите новое содержимое папки `out/` на FTP (можно заменить только измененные файлы)

## ✅ Проверка:

После загрузки проверьте:
- [ ] Сайт открывается: `https://yourdomain.com`
- [ ] Редирект на `/en` работает
- [ ] Все языки доступны: `/es`, `/ru`, `/ua`, и т.д.
- [ ] Изображения загружаются
- [ ] Навигация работает
- [ ] WhatsApp кнопка работает

## 🆘 Проблемы:

### Страницы показывают 404:
- Убедитесь, что загружены ВСЕ файлы из `out/`
- Проверьте, что `.htaccess` настроен правильно (если нужен)

### Изображения не загружаются:
- Проверьте, что папка `public/` была скопирована в `out/`
- Проверьте пути к изображениям

### Языки не переключаются:
- Убедитесь, что все языковые папки загружены
- Проверьте, что `middleware.ts` не требуется (для статического экспорта)

## 📝 Примечание:

Статический экспорт создает полностью автономный сайт, который работает на любом веб-сервере, включая обычный FTP хостинг без Node.js.

