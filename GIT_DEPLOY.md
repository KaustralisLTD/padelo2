# 📦 Загрузка в Git и деплой на Vercel

## ✅ Git репозиторий инициализирован!

Код готов к загрузке на GitHub.

---

## 📋 Шаги:

### 1. Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com) и войдите
2. Нажмите **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name:** `padelo2` (или любое имя)
   - **Description:** `PadelO2 - Multi-language padel sports ecosystem website`
   - **Visibility:** `Public` или `Private` (на ваше усмотрение)
   - **НЕ** добавляйте:
     - ❌ README
     - ❌ .gitignore
     - ❌ License
4. Нажмите **"Create repository"**

### 2. Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
cd "/Users/spilberg/AI Projects/PadelO2"

# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/padelo2.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Или если используете SSH:**

```bash
git remote add origin git@github.com:YOUR_USERNAME/padelo2.git
git branch -M main
git push -u origin main
```

### 3. Подключите в Vercel

1. Зайдите в [vercel.com/dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New Project"** или **"Import Project"**
3. Выберите **GitHub** как источник
4. Если нужно, авторизуйтесь через GitHub
5. Найдите ваш репозиторий `padelo2` в списке
6. Нажмите **"Import"**

### 4. Настройте проект в Vercel

Vercel автоматически определит:
- ✅ Framework: Next.js
- ✅ Build Command: `npm run build`
- ✅ Output Directory: `.next`
- ✅ Install Command: `npm install`

**Просто нажмите "Deploy"!**

### 5. Готово! 🎉

После деплоя:
- Сайт будет доступен по ссылке типа `https://padelo2.vercel.app`
- Каждый раз при `git push` будет автоматический деплой
- Можно настроить свой домен в настройках проекта

---

## 🔄 Обновление сайта в будущем:

```bash
# Внесите изменения в код
# Затем:
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически задеплоит обновления!

---

## ✅ Проверка:

После деплоя проверьте:
- [ ] Сайт открывается
- [ ] Все языки работают (`/en`, `/es`, `/ru`, и т.д.)
- [ ] Изображения загружаются
- [ ] Навигация работает
- [ ] WhatsApp кнопка работает

