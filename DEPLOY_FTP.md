# Инструкция по деплою на FTP (Ukraine.com.ua)

## 🔐 Данные для доступа:
- **Host:** `foldis00.ftp.tools`
- **Username:** `foldis00_padel`
- **Password:** `pM6ziY9ozU`
- **Хостинг:** Ukraine.com.ua (поддерживает Node.js и Next.js)

## 📚 Официальная документация:
- [Node.js на Ukraine.com.ua](https://www.ukraine.com.ua/wiki/hosting/nodejs/)
- [Next.js на Ukraine.com.ua](https://www.ukraine.com.ua/wiki/hosting/nodejs/nextjs/)

## ✅ Хорошие новости!

Ваш хостинг **Ukraine.com.ua поддерживает Node.js и Next.js**, поэтому можно использовать полный функционал Next.js с App Router!

## 📦 Процесс деплоя:

### Шаг 1: Подготовка проекта локально

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите production build
npm run build
```

### Шаг 2: Загрузка файлов на FTP

#### Вариант A: Автоматический скрипт

```bash
npm run deploy:ftp
# или
./deploy-ftp.sh
```

#### Вариант B: Ручная загрузка через FileZilla

1. **Подключитесь к FTP:**
   - Host: `foldis00.ftp.tools`
   - Username: `foldis00_padel`
   - Password: `pM6ziY9ozU`
   - Port: `21`

2. **Загрузите следующие файлы и папки:**
   - `.next/` - папка со сборкой (важно!)
   - `public/` - статические файлы (изображения, видео, логотипы)
   - `messages/` - папка с переводами (14 языков)
   - `package.json`
   - `package-lock.json` (если есть)
   - `next.config.mjs`
   - `i18n.ts`
   - `middleware.ts`
   - `tsconfig.json`
   - `tailwind.config.ts`
   - `postcss.config.js`
   - `app/` - исходные файлы приложения
   - `components/` - компоненты React

   **НЕ загружайте:**
   - `node_modules/` (установится на сервере)
   - `.git/`
   - `.next/cache/` (опционально, можно не загружать)

### Шаг 3: Настройка на сервере через панель управления

Согласно документации Ukraine.com.ua, вам нужно:

1. **Войти в панель управления хостингом**

2. **Активировать Node.js:**
   - Перейдите в раздел "Node.js" в панели управления
   - Создайте новое Node.js приложение
   - Укажите версию Node.js (рекомендуется 18+ или 20+)
   - Укажите рабочую директорию (где загружены файлы)

3. **Установите зависимости на сервере:**
   
   Через SSH или консоль в панели управления:
   ```bash
   cd /path/to/your/app
   npm install --production
   ```

4. **Настройте запуск приложения:**
   
   В настройках Node.js приложения укажите:
   - **Start Command:** `npm start`
   - **Working Directory:** путь к вашей директории
   - **Port:** обычно автоматически определяется

### Структура на сервере должна быть:
```
/
├── .next/          # Сборка Next.js (загрузить)
├── public/         # Статические файлы (загрузить)
├── messages/       # Переводы (загрузить)
├── app/            # Исходники (загрузить)
├── components/     # Компоненты (загрузить)
├── node_modules/   # Зависимости (установить на сервере)
├── package.json    # Загрузить
├── next.config.mjs # Загрузить
├── i18n.ts         # Загрузить
├── middleware.ts   # Загрузить
└── ...
```

## 🔧 Дополнительные настройки:

### Настройка переменных окружения (если нужно):

Если в будущем понадобятся переменные окружения, создайте файл `.env.production` на сервере:
```env
NODE_ENV=production
```

### Настройка домена:

1. В панели управления хостингом настройте домен
2. Убедитесь, что домен указывает на ваше Node.js приложение
3. Хостинг автоматически настроит проксирование на ваш Next.js сервер

## ✅ Проверка после деплоя:

После деплоя проверьте:
- [ ] Сайт открывается по домену
- [ ] Все языки работают (`/en`, `/es`, `/ru`, и т.д.)
- [ ] Изображения загружаются
- [ ] Формы работают (проверьте отправку на email)
- [ ] WhatsApp кнопка работает
- [ ] Все страницы доступны

## 🆘 Решение проблем:

### Сайт не открывается:
1. Проверьте, что Node.js приложение запущено в панели управления
2. Проверьте логи в панели управления
3. Убедитесь, что порт настроен правильно

### Ошибка "Cannot find module":
```bash
# На сервере выполните:
cd /path/to/your/app
npm install --production
```

### Ошибка "Module not found" для next-intl:
- Убедитесь, что `messages/` папка загружена полностью
- Проверьте, что все файлы локализации присутствуют

### Изображения не загружаются:
- Убедитесь, что папка `public/` загружена полностью
- Проверьте пути к изображениям (должны начинаться с `/`)

### Проблемы с локализацией:
- Проверьте, что `middleware.ts` загружен
- Убедитесь, что все файлы в `messages/` присутствуют
- Проверьте конфигурацию в `i18n.ts`

## 📞 Поддержка:

Если возникнут проблемы:
1. Проверьте [документацию Ukraine.com.ua по Next.js](https://www.ukraine.com.ua/wiki/hosting/nodejs/nextjs/)
2. Обратитесь в техподдержку хостинга
3. Проверьте логи приложения в панели управления

## 🚀 Быстрый старт:

```bash
# 1. Локально
npm run build

# 2. Загрузите файлы через FileZilla или скрипт
npm run deploy:ftp

# 3. На сервере через панель управления:
# - Активируйте Node.js
# - Установите зависимости: npm install --production
# - Запустите приложение: npm start
```

---

**Примечание:** Если у вас нет доступа к SSH, все операции можно выполнить через панель управления хостингом Ukraine.com.ua.
