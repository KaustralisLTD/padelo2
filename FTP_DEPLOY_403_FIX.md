# 🔧 Решение ошибки 403 Forbidden на FTP-хостинге

## 📋 Проблема

Ошибка 403 Forbidden на FTP-хостинге (hosting.xyz) возникает потому, что:
1. ❌ Отсутствует файл `index.html` в корне сайта
2. ❌ Файлы размещены неправильно
3. ❌ Next.js требует Node.js, которого нет на обычном хостинге

## ✅ Решение: Статический экспорт

Создайте статическую версию сайта, которая работает без Node.js.

## 🚀 Пошаговая инструкция

### Шаг 1: Соберите статический сайт

```bash
npm run build:static
```

Этот скрипт:
- ✅ Временно включает статический экспорт
- ✅ Собирает сайт в папку `out/`
- ✅ Восстанавливает конфигурацию для разработки

### Шаг 2: Проверьте содержимое папки `out/`

После сборки в папке `out/` должны быть:
```
out/
├── index.html          ← Главный файл (должен быть!)
├── en/
│   ├── index.html
│   ├── about/
│   └── ...
├── _next/              ← Скрипты и стили
│   ├── static/
│   └── ...
└── images/             ← Изображения (из public/)
```

### Шаг 3: Загрузите файлы на FTP

**Важно:** Загрузите **ВСЁ содержимое** папки `out/` в корневую директорию вашего сайта.

#### Через FileZilla:

1. **Подключитесь к FTP:**
   - Host: `foldis00.ftp.tools`
   - User: `foldis00_padel`
   - Pass: `pM6ziY9ozU`

2. **Найдите корневую директорию сайта:**
   - Обычно это `public_html/` или `www/`
   - Или `example.com/www/` (где `example.com` - имя вашего сайта)

3. **Загрузите файлы:**
   - Выберите **все файлы и папки** из `out/`
   - Перетащите их в корневую директорию
   - **Убедитесь, что `index.html` находится в корне!**

### Шаг 4: Проверьте права доступа

Установите правильные права:
- **Файлы:** `644` (rw-r--r--)
- **Папки:** `755` (rwxr-xr-x)

В FileZilla: ПКМ на файле → File permissions → Установите `644` для файлов, `755` для папок.

### Шаг 5: Проверьте структуру на сервере

После загрузки структура должна быть:
```
Корень сайта (public_html/ или www/)/
├── index.html          ← Должен быть здесь!
├── en/
│   ├── index.html
│   └── ...
├── _next/
└── images/
```

## ⚠️ Важные замечания

### 1. Правильное размещение файлов

Согласно [документации hosting.xyz](https://hosting.xyz/wiki/hosting/sites/errors/403/):
- Основные файлы сайта должны быть в `example.com/www/`
- Файл `index.html` должен быть в корне этой директории

### 2. Ограничения статического экспорта

❌ **Не будет работать:**
- API Routes (формы не отправят данные)
- Серверный рендеринг
- Динамические маршруты

✅ **Будет работать:**
- Все страницы и языки
- Навигация
- Изображения и видео
- Анимации

### 3. Обновление сайта

После каждого изменения:
1. Запустите `npm run build:static`
2. Загрузите новое содержимое папки `out/` на FTP
3. Замените старые файлы новыми

## 🔍 Диагностика ошибки 403

Если ошибка 403 сохраняется после загрузки:

### 1. Проверьте наличие index.html

Убедитесь, что файл `index.html` находится в корне сайта:
- В панели управления хостингом проверьте структуру файлов
- Или через FTP проверьте, что `index.html` в корневой директории

### 2. Проверьте .htaccess

Если есть файл `.htaccess` в корне:
- Убедитесь, что нет директив, блокирующих доступ
- Убедитесь, что нет редиректов на несуществующие директории

### 3. Проверьте ограничения доступа

В панели управления хостингом:
- Проверьте настройки ограничения доступа
- Убедитесь, что ваш IP не заблокирован
- Проверьте настройки User-Agent блокировки

### 4. Проверьте права доступа

Убедитесь, что:
- Файлы имеют права `644`
- Папки имеют права `755`
- Владелец файлов правильный

### 5. Проверьте логи

В панели управления хостингом посмотрите логи доступа:
- Найдите запросы, которые возвращают 403
- Проверьте, какой файл запрашивается

## 📞 Если проблема не решена

1. **Проверьте структуру файлов** - убедитесь, что всё загружено правильно
2. **Обратитесь в поддержку хостинга** - они могут проверить настройки сервера
3. **Проверьте логи** - найдите конкретную причину ошибки 403

## 🎯 Альтернативное решение: Vercel

Если проблемы с FTP продолжаются, рекомендую использовать **Vercel**:
- ✅ Автоматический деплой через Git
- ✅ Не требует настройки
- ✅ Работает с Next.js из коробки
- ✅ Бесплатный план для начала

Инструкция по деплою на Vercel: `VERCEL_DEPLOY_STEPS.md`

