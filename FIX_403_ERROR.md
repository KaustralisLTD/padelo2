# 🔧 Решение ошибки 403 Forbidden

## 📍 Где возникает ошибка?

### Вариант 1: Ошибка на Vercel

Если ошибка 403 возникает на Vercel после деплоя:

1. **Проверьте настройки проекта в Vercel:**
   - Framework Preset: `Next.js`
   - Build Command: `npm run build`
   - Output Directory: `.next` (по умолчанию)
   - Install Command: `npm install`

2. **Проверьте, что репозиторий правильно подключен:**
   - Убедитесь, что код запушен в GitHub
   - Проверьте, что Vercel подключен к правильной ветке (`main`)

3. **Пересоберите проект:**
   - В Vercel Dashboard: Settings → Deployments → Redeploy

### Вариант 2: Ошибка на FTP-хостинге

Если вы пытаетесь загрузить сайт на FTP-хостинг (hosting.xyz), вам нужен **статический экспорт**, так как у вас нет Node.js на хостинге.

## 🚀 Решение для FTP-хостинга

### Шаг 1: Включите статический экспорт

Раскомментируйте настройки в `next.config.mjs`:

```javascript
const nextConfig = {
  output: 'export', // ← Раскомментируйте
  images: {
    unoptimized: true, // ← Раскомментируйте
    domains: [],
    formats: ['image/avif', 'image/webp'],
  },
  trailingSlash: true, // ← Раскомментируйте
};
```

### Шаг 2: Соберите статический сайт

```bash
npm run build
```

После сборки будет создана папка `out/` со всеми статическими файлами.

### Шаг 3: Загрузите на FTP

**Важно:** Загрузите **ВСЁ содержимое** папки `out/` в корневую директорию вашего сайта:

- Обычно это `public_html/` или `www/`
- Убедитесь, что файл `index.html` находится в корне
- Загрузите также папку `_next/` и все файлы из `public/`

### Шаг 4: Проверьте права доступа

Убедитесь, что файлы имеют правильные права:
- Файлы: `644` (rw-r--r--)
- Папки: `755` (rwxr-xr-x)

## ⚠️ Важные замечания

### Для статического экспорта:

1. **Формы не будут работать** - они требуют серверной части
   - Используйте внешние сервисы (Formspree, EmailJS) или `mailto:` ссылки

2. **Только статические страницы** - динамические маршруты не работают

3. **После изменений** нужно пересобрать и загрузить заново

### Для Vercel (рекомендуется):

Vercel автоматически обрабатывает Next.js и не требует статического экспорта. Просто:
1. Запушьте код в GitHub
2. Подключите репозиторий в Vercel
3. Деплой произойдет автоматически

## 🔍 Диагностика

Если ошибка 403 сохраняется:

1. **Проверьте наличие index.html:**
   - Для статического экспорта: должен быть в `out/index.html`
   - Для Vercel: генерируется автоматически

2. **Проверьте структуру файлов:**
   ```
   Корень сайта/
   ├── index.html          ← Должен быть!
   ├── _next/              ← Папка со скриптами
   └── images/            ← Изображения
   ```

3. **Проверьте .htaccess (если есть):**
   - Убедитесь, что нет директив, блокирующих доступ
   - Убедитесь, что нет редиректов на несуществующие директории

4. **Проверьте логи хостинга:**
   - В панели управления хостингом посмотрите логи доступа
   - Найдите запросы, которые возвращают 403

## 📞 Нужна помощь?

Если проблема не решена:
1. Уточните, где именно возникает ошибка (Vercel или FTP)
2. Проверьте логи хостинга
3. Убедитесь, что файлы загружены правильно

