# PadelO₂ - Статус проекта и план реализации

> **Документ для перехода в новый чат**  
> Содержит выдержку из всего чата, описание реализованного функционала, расположение файлов и детальный план реализации задач.

## 📋 Общая информация

**Проект**: PadelO₂ - Платформа для управления турнирами по паделю  
**Технологии**: Next.js 14 (App Router), TypeScript, TailwindCSS, MySQL, next-intl (14 языков)  
**База данных**: MySQL (foldis00.mysql.tools)  
**Языки**: EN, ES, RU, UA, CA, DE, FR, IT, NL, DA, SV, NO, ZH, AR

**Учетные данные Super Admin**:
- Email: `admin@padelo2.com`
- Password: `admin123`

---

## ✅ Реализованные функции

### 1. Аутентификация и авторизация
- ✅ Регистрация и вход пользователей
- ✅ Роли: `superadmin`, `staff`, `participant`
- ✅ Управление сессиями
- ✅ Защита маршрутов по ролям

### 2. Админ-панель (Super Admin)
- ✅ **Управление пользователями** (`/admin/users`)
  - Создание, редактирование, удаление пользователей
  - Назначение ролей
  - Вкладки для просмотра данных пользователя:
    - Bookings (бронирования/регистрации на турниры)
    - Matches (матчи пользователя)
    - Wallet (кошелек и транзакции)
    - Events (турниры/события)
    - Trainings (тренировки - заглушка)
    - Profile (профиль и статистика)
- ✅ **Управление турнирами** (`/admin/tournaments`)
  - CRUD операции для турниров
  - Редактирование правил турнира
  - Управление статусами
  - Адрес проведения с Google Maps
  - Расписание событий (event_schedule)
- ✅ **Управление персоналом** (`/admin/staff`)
  - Назначение доступа к турнирам
  - Управление правами (группы, матчи, регистрации)
  - Фильтрация и поиск по списку доступов
  - Статистика по сотрудникам
  - Копирование прав доступа между пользователями
  - Массовое назначение доступа к нескольким турнирам
- ✅ **Журнал аудита** (`/admin/logs`)
  - Просмотр всех действий пользователей
  - Фильтрация и поиск
  - Логирование входов, выходов, действий
- ✅ **Управление кошельками** (`/admin/wallet`)
  - Просмотр всех кошельков пользователей
  - Просмотр всех транзакций
  - Вкладки: Wallets и Transactions

### 3. Турниры
- ✅ Регистрация на турниры
- ✅ Распределение по группам
- ✅ Турнирная таблица (Bracket)
  - Перетаскивание пар между группами
  - Редактирование пар
  - Ручной ввод игроков
  - Визуальная индикация ошибок в группах
- ✅ Расписание игр (Schedule)
  - Генерация расписания для всех категорий
  - Множественные временные промежутки
  - Управление длительностью матчей (1-4 слота)
  - Drag & drop для изменения времени и корта
  - Ввод результатов матчей
- ✅ Правила турнира (локализованы)
- ✅ Финальная часть (Knockout Stage)

### 4. Профиль пользователя
- ✅ Редактирование профиля
- ✅ Загрузка фото (только для партнера)
- ✅ Участник Dashboard
- ✅ Выход из системы

### 5. Локализация
- ✅ 14 языков полностью переведены
- ✅ Все компоненты локализованы
- ✅ SEO метаданные для всех языков

### 6. SEO
- ✅ Schema.org разметка
- ✅ Sitemap
- ✅ Robots.txt
- ✅ Hreflang теги

### 7. Логирование (Audit Logs)
- ✅ Логирование всех действий пользователей
- ✅ Логирование входов/выходов
- ✅ Логирование ошибок
- ✅ IP-адрес и User-Agent

---

## 📁 Расположение ключевых файлов

### Инструкции по настройке
- `DATABASE_SETUP_UKRAINE.md` - Настройка базы данных MySQL
- `CREATE_ADMIN_NOW.md` - Инструкция по созданию админа вручную

### API Endpoints
- `app/api/auth/` - Аутентификация (login, logout, register)
- `app/api/admin/` - Админ-панель (users, tournaments, staff, logs)
- `app/api/tournament/[id]/` - Управление турнирами
  - `bracket/` - Турнирная таблица
  - `schedule/` - Расписание игр
  - `pair/` - Управление парами
  - `match/` - Результаты матчей

### Компоненты
- `components/pages/AdminUsersContent.tsx` - Управление пользователями
- `components/pages/AdminTournamentsContent.tsx` - Управление турнирами
- `components/pages/AdminStaffContent.tsx` - Управление персоналом
- `components/pages/AdminLogsContent.tsx` - Журнал аудита
- `components/TournamentBracket.tsx` - Турнирная таблица
- `components/TournamentSchedule.tsx` - Расписание игр
- `components/TournamentScheduleAdmin.tsx` - Админ-панель расписания

### Утилиты
- `lib/db.ts` - Подключение к БД
- `lib/users.ts` - Управление пользователями
- `lib/audit-log.ts` - Логирование действий
- `lib/schedule.ts` - Генерация расписания
- `lib/schedule-time-slots.ts` - Генерация с временными промежутками

### Скрипты
- `scripts/create-admin-direct.ts` - Создание админа напрямую в БД
- `scripts/distribute-test-participants.ts` - Распределение тестовых участников

---

## 🔧 Известные проблемы

### ✅ Исправленные проблемы (2025-11-15)

#### Исправление дублирования кнопки "Приховати розклад"
- **Статус**: ✅ Исправлено
- **Описание**: Убрано дублирование кнопки, оставлена только в `TournamentScheduleAdmin` справа вместе с кнопкой темы
- **Файлы**: `components/TournamentBracket.tsx`, `components/TournamentScheduleAdmin.tsx`

#### Исправление генерации расписания для финального этапа
- **Статус**: ✅ Исправлено
- **Описание**: Исправлена обработка матчей с `match_date = NULL`, матчи полуфинала теперь отображаются в календаре и списке с правильным временем
- **Файлы**: `lib/schedule.ts`

#### Исправление конфликта маршрутов API
- **Статус**: ✅ Исправлено
- **Описание**: Устранен конфликт между `[id]` и `[userId]` в `app/api/admin/users/`, все подмаршруты объединены в `[id]`
- **Файлы**: `app/api/admin/users/[id]/*` (все подмаршруты)

---

## 🔧 Текущие проблемы

### 1. Лимиты подключений к БД (50 соединений)
**Проблема**: Постоянно превышается лимит в 50 соединений к MySQL  
**Текущее решение**: `connectionLimit: 3` в `lib/db.ts`  
**Симптомы**: 
- Ошибка `ER_TOO_MANY_USER_CONNECTIONS`
- Невозможность создать админа через скрипт
- Медленная работа API

**Требуется**: 
- Проверить все места, где открываются соединения к БД
- Убедиться, что все соединения закрываются через `pool.execute()` или `connection.release()`
- Добавить мониторинг активных соединений
- Проверить, нет ли утечек в API endpoints
- Оптимизировать пул соединений
- Добавить автоматическое закрытие неактивных соединений

**Файлы для проверки**:
- `lib/db.ts` - настройки пула (`connectionLimit: 3`)
- Все файлы в `app/api/` - использование `getDbPool()`

**SQL для диагностики**:
```sql
SHOW PROCESSLIST; -- Показать активные соединения
SHOW VARIABLES LIKE 'max_connections';
SHOW VARIABLES LIKE 'max_user_connections';
```

### 2. Выброс из СуперАдмина
**Проблема**: После входа админ постоянно выходит из системы, затем не может войти повторно  
**Симптомы**:
- "Invalid email or password" после успешного входа
- Сессия не сохраняется
- Токен не работает при повторных запросах

**Требуется**:
- Проверить логику сессий в `lib/users.ts` (`createSession`, `getSession`, `deleteSession`)
- Проверить срок действия токенов (сейчас 7 дней)
- Проверить, не удаляются ли сессии случайно
- Проверить cookies и localStorage
- Проверить middleware для проверки авторизации
- Добавить логирование при проверке сессий

**Файлы для проверки**:
- `lib/users.ts` - функции сессий
- `app/api/auth/login/route.ts` - логика входа
- Компоненты, использующие проверку авторизации

**SQL для диагностики**:
```sql
SELECT * FROM sessions WHERE user_id = (SELECT id FROM users WHERE email = 'admin@padelo2.com');
```

### 3. Генерация расписания не работает ✅ **ИСПРАВЛЕНО**
**Проблема**: При генерации расписания возникает ошибка 500  
**Симптомы**:
- `GET http://localhost:3000/api/tournament/2/schedule 500 (Internal Server Error)`
- Кнопка "Generate" становится неактивной
- Расписание не создается
- Матчи финального этапа (полуфиналы) не отображались в календаре и списке
- Время матчей устанавливалось неправильно (01:04 вместо нормального времени)

**Исправлено**:
- ✅ Обновлена функция `saveSchedule` в `lib/schedule.ts` для правильной обработки матчей с `match_date = NULL`
- ✅ Добавлена проверка формата времени при сохранении (Date объект или строка)
- ✅ Матчи полуфинала теперь правильно обновляются при генерации расписания
- ✅ Время устанавливается корректно из `timeSlots`

**Файлы изменены**:
- `lib/schedule.ts` - улучшена обработка матчей с NULL временем

### 4. Audit Logs не отображаются
**Проблема**: На странице `/admin/logs` не показываются логи (вход в систему, перемещение пар и т.д.)  
**Симптомы**:
- Таблица логов пустая
- Нет записей о входе/выходе
- Нет записей о действиях пользователей

**Требуется**:
- Проверить, создается ли таблица `audit_logs` в БД
- Проверить API endpoint `GET /api/admin/logs`
- Проверить функцию `getAuditLogs` в `lib/audit-log.ts`
- Проверить, правильно ли работает фильтрация
- Добавить логирование ошибок в компоненте `AdminLogsContent.tsx`
- Проверить права доступа (только superadmin)
- Проверить, вызывается ли `logAction` во всех нужных местах

**Файлы для проверки**:
- `app/api/admin/logs/route.ts`
- `lib/audit-log.ts`
- `components/pages/AdminLogsContent.tsx`
- `lib/db.ts` - создание таблицы `audit_logs`

**SQL для диагностики**:
```sql
SELECT * FROM audit_logs ORDER BY created_at DESC LIMIT 10;
SHOW CREATE TABLE audit_logs;
```

### 5. Очень медленная работа сайта и БД
**Проблема**: При входе в аккаунт и других действиях очень долго работает сайт или взаимосвязь с БД, Loading... очень долго, хотя вход должен происходить мгновенно  
**Симптомы**:
- Долгая загрузка при входе
- Долгие запросы к API
- Медленная работа всех операций с БД

**Требуется**:
- Провести профилирование запросов к БД
- Оптимизировать SQL запросы (добавить индексы)
- Проверить, нет ли N+1 запросов
- Добавить кэширование для часто используемых данных
- Оптимизировать пул соединений
- Проверить, не блокируются ли запросы
- Добавить мониторинг времени выполнения запросов
- Использовать `Promise.all()` для параллельных запросов где возможно

**Файлы для проверки**:
- Все файлы в `app/api/` - проверить запросы к БД
- `lib/db.ts` - оптимизация пула
- `lib/users.ts` - оптимизация запросов пользователей
- Компоненты с медленной загрузкой

**SQL для диагностики**:
```sql
-- Проверить медленные запросы
SHOW FULL PROCESSLIST;
-- Проверить индексы
SHOW INDEX FROM users;
SHOW INDEX FROM tournaments;
SHOW INDEX FROM sessions;
```

### 6. Потеря ID пар при перемещении между группами
**Проблема**: При перемещении (перетягивании) пар между группами у них меняется ID пары (Pair 7, например), в результате в категории "Women's 1" может быть две Pair 7, две Pair 8 и две Pair 10  
**Симптомы**:
- Дублирование номеров пар в одной категории
- Усложнение дальнейшей работы
- Потеря уникальности идентификаторов

**Требуется**:
- Сохранять уникальный ID пары при перемещении между группами
- Использовать `pair_id` из таблицы `tournament_group_pairs` как постоянный идентификатор
- Обновить логику перемещения пар, чтобы не пересчитывать `pair_number`
- Добавить проверку уникальности `pair_number` в группе
- Обновить визуальное отображение (показывать `pair_id` или уникальный номер)

**Файлы для проверки**:
- `components/TournamentBracket.tsx` - логика перемещения пар
- `app/api/tournament/[id]/bracket/route.ts` - API для перемещения
- `lib/matches.ts` - функции работы с парами

---

## 📝 План реализации

### Приоритет 1: Критические проблемы

#### 1.1 Разобраться с БД и лимитами подключений
**Задачи**:
- [ ] Проверить все места, где открываются соединения к БД
- [ ] Убедиться, что все соединения закрываются через `pool.execute()` или `connection.release()`
- [ ] Добавить мониторинг активных соединений
- [ ] Проверить, нет ли утечек в API endpoints
- [ ] Оптимизировать пул соединений (увеличить `connectionLimit` если нужно)
- [ ] Добавить автоматическое закрытие неактивных соединений

**Файлы для проверки**:
- `lib/db.ts` - настройки пула
- Все файлы в `app/api/` - использование `getDbPool()`

#### 1.2 Проблема с выбросом из СуперАдмина
**Задачи**:
- [ ] Проверить логику создания сессий в `lib/users.ts` (`createSession`, `getSession`)
- [ ] Проверить срок действия токенов (сейчас 7 дней)
- [ ] Проверить middleware для проверки авторизации
- [ ] Добавить логирование при проверке сессий
- [ ] Проверить, не удаляются ли сессии случайно
- [ ] Проверить cookies и localStorage

**Файлы для проверки**:
- `lib/users.ts` - функции сессий
- `app/api/auth/login/route.ts` - логика входа
- Компоненты, использующие проверку авторизации

#### 1.3 Генерация расписания не работает ✅ **ВЫПОЛНЕНО**
**Задачи**:
- [x] Проверить логи сервера при генерации
- [x] Проверить `lib/schedule-time-slots.ts` на ошибки
- [x] Проверить API endpoint `POST /api/tournament/[id]/schedule`
- [x] Проверить, правильно ли передаются `timeSlots`
- [x] Проверить, есть ли все необходимые данные в БД (группы, пары)
- [x] Добавить детальное логирование ошибок
- [x] Исправить обновление матчей с `match_date = NULL` для финального этапа
- [x] Исправить установку времени при генерации расписания

**Файлы изменены**:
- `lib/schedule.ts` - улучшена обработка матчей с NULL временем и формат времени

#### 1.4 Audit Logs не отображаются
**Задачи**:
- [ ] Проверить, создается ли таблица `audit_logs` в БД
- [ ] Проверить API endpoint `GET /api/admin/logs`
- [ ] Проверить функцию `getAuditLogs` в `lib/audit-log.ts`
- [ ] Проверить, правильно ли работает фильтрация
- [ ] Добавить логирование ошибок в компоненте `AdminLogsContent.tsx`
- [ ] Проверить права доступа (только superadmin)

**Файлы для проверки**:
- `app/api/admin/logs/route.ts`
- `lib/audit-log.ts`
- `components/pages/AdminLogsContent.tsx`
- `lib/db.ts` - создание таблицы `audit_logs`

#### 1.5 Оптимизация производительности (медленная работа сайта)
**Задачи**:
- [ ] Провести профилирование всех запросов к БД
- [ ] Добавить индексы на часто используемые поля:
  - `users.email`, `users.role`
  - `sessions.token`, `sessions.user_id`
  - `tournament_registrations.tournament_id`, `tournament_registrations.user_id`
  - `tournament_groups.tournament_id`, `tournament_groups.category`
- [ ] Оптимизировать запросы (убрать N+1 проблемы)
- [ ] Добавить кэширование для часто используемых данных (Redis или in-memory)
- [ ] Использовать `Promise.all()` для параллельных запросов
- [ ] Добавить мониторинг времени выполнения запросов
- [ ] Оптимизировать компоненты (lazy loading, memoization)
- [ ] Проверить размер ответов API (минимизировать данные)

**Файлы для изменения**:
- `lib/db.ts` - добавить индексы при создании таблиц
- Все файлы в `app/api/` - оптимизировать запросы
- `lib/users.ts` - оптимизировать запросы пользователей
- Компоненты с медленной загрузкой

#### 1.6 Исправление потери ID пар при перемещении
**Задачи**:
- [ ] Исправить логику перемещения пар, чтобы сохранять уникальный `pair_id`
- [ ] Использовать `id` из `tournament_group_pairs` как постоянный идентификатор
- [ ] Обновить API endpoint для перемещения пар
- [ ] Добавить проверку уникальности `pair_number` в группе
- [ ] Обновить визуальное отображение (показывать уникальный ID)
- [ ] Добавить миграцию для исправления существующих дубликатов

**Файлы для изменения**:
- `components/TournamentBracket.tsx` - логика перемещения
- `app/api/tournament/[id]/bracket/route.ts` - API для перемещения
- `lib/matches.ts` - функции работы с парами

### Приоритет 2: Улучшения UX и функционала

#### 2.1 Кнопка Login → Account после входа
**Текущее состояние**: В `Header.tsx` уже есть логика для изменения "Login" на "Profile"  
**Требуется**: Изменить "Profile" на "Account"  
**Задачи**:
- [ ] Изменить ключ локализации с `profile` на `account` в `Header.tsx`
- [ ] Добавить ключ `account` в `Navigation` для всех языков
- [ ] Оставить ссылку на `/profile` (меняется только текст)

**Файлы для изменения**:
- `components/Header.tsx` (строки 115, 184)
- `messages/*.json` - добавить/изменить ключ `account` в `Navigation`

#### 2.2 Меню Dashboard в основном меню после входа
**Задачи**:
- [ ] Проверить компонент `Header.tsx`
- [ ] Добавить условный рендеринг пункта меню "Dashboard"
- [ ] Показывать только для авторизованных пользователей
- [ ] Добавить локализацию для всех языков
- [ ] Ссылка должна вести на `/dashboard`

**Файлы для изменения**:
- `components/Header.tsx`
- `messages/*.json` - добавить ключ для "Dashboard"

#### 2.3 Детальная информация о турнире (адрес, расписание событий)
**Требуется**: В настройках турнира (Tournament Details) добавить:
- Поле адреса проведения с подвязкой к Google Maps
- Детальное расписание событий (лотерея, автограф-сессии, приезд звезд и т.д.)
- Возможность редактирования для СуперАдмина и Админа

**Задачи**:
- [ ] Добавить поле `location_address` в таблицу `tournaments` (TEXT)
- [ ] Добавить поле `location_coordinates` (JSON: `{lat, lng}`)
- [ ] Добавить поле `event_schedule` (JSON) для хранения расписания событий:
  ```json
  [
    {
      "date": "2025-12-19",
      "time": "10:00",
      "event": "Лотерея",
      "description": "Жеребьевка групп"
    },
    {
      "date": "2025-12-20",
      "time": "14:00",
      "event": "Автограф-сессия",
      "description": "Приезд звезд падела"
    }
  ]
  ```
- [ ] Обновить форму редактирования турнира:
  - Поле ввода адреса с автодополнением (Google Places API)
  - Карта Google Maps для выбора координат
  - Редактор расписания событий (добавление/удаление/редактирование)
- [ ] Обновить страницу деталей турнира:
  - Отображение карты с маркером
  - Кнопка "Проложить маршрут" (открывает Google Maps)
  - Отображение расписания событий
- [ ] Добавить проверку прав доступа (superadmin, tournament_admin)
- [ ] Добавить локализацию для всех языков

**Файлы для изменения**:
- `components/pages/AdminTournamentsContent.tsx` - форма редактирования
- `components/TournamentDetails.tsx` - отображение деталей
- `app/api/admin/tournaments/route.ts` - обновить API
- `lib/db.ts` - обновить схему БД
- `messages/*.json` - локализация

**Зависимости**:
- Google Maps API ключ (добавить в `.env`)
- `@react-google-maps/api` или `google-maps-react` для React

#### 2.4 Улучшение редактирования пар в Tournament Bracket
**Текущая проблема**: При редактировании пары нельзя выбрать игроков из выпадающего списка, можно редактировать только из зарегистрированных участников  
**Требуется**: 
- Добавить возможность редактировать имена игроков напрямую
- Добавить возможность дописать нового игрока (не существующего в БД)
- Добавить кнопку редактирования справа возле имени каждого игрока
- Перед сохранением добавить галочку "Уведомить участников об изменениях"
- Отправка уведомлений через WhatsApp и Email

**Задачи**:
- [ ] Обновить модальное окно редактирования пары:
  - Поля ввода для имен игроков (не только выбор из списка)
  - Возможность ввести новое имя (не из БД)
  - Чекбокс "Уведомить участников об изменениях"
- [ ] Добавить кнопки редактирования рядом с именами игроков в паре
- [ ] Обновить API endpoint для сохранения изменений:
  - Сохранение новых имен игроков
  - Создание временных записей для несуществующих игроков
- [ ] Добавить отправку уведомлений:
  - Email через `lib/email.ts` или SendGrid
  - WhatsApp через API (Twilio или другой сервис)
- [ ] Добавить шаблоны уведомлений (локализованные)
- [ ] Логировать изменения пар в `audit_logs`

**Файлы для изменения**:
- `components/TournamentBracket.tsx` - модальное окно редактирования
- `app/api/tournament/[id]/pair/route.ts` - API для обновления пары
- `lib/email.ts` - отправка email уведомлений
- `lib/whatsapp.ts` - отправка WhatsApp (создать новый файл)
- `messages/*.json` - локализация

#### 2.5 Вкладки в разделе Users
**Требуется**: В категории Users добавить вкладки:
- `bookings` - Бронирования
- `matches` - Матчи
- `wallet` - Кошелек
- `events` - События
- `trainings` - Тренировки
- `profile` - Профиль

**Задачи**:
- [ ] Обновить компонент `AdminUsersContent.tsx`:
  - Добавить табы для переключения между разделами
  - Создать компоненты для каждого раздела:
    - `UserBookings.tsx` - бронирования пользователя
    - `UserMatches.tsx` - матчи пользователя
    - `UserWallet.tsx` - кошелек пользователя
    - `UserEvents.tsx` - события пользователя
    - `UserTrainings.tsx` - тренировки пользователя
    - `UserProfile.tsx` - профиль пользователя (уже существует)
- [ ] Создать API endpoints для каждого раздела:
  - `GET /api/admin/users/[id]/bookings`
  - `GET /api/admin/users/[id]/matches`
  - `GET /api/admin/users/[id]/wallet`
  - `GET /api/admin/users/[id]/events`
  - `GET /api/admin/users/[id]/trainings`
- [ ] Добавить локализацию для всех языков
- [ ] Добавить права доступа (superadmin, staff с правами)

**Файлы для изменения**:
- `components/pages/AdminUsersContent.tsx` - добавить табы
- `components/pages/users/` - создать компоненты для каждого раздела
- `app/api/admin/users/[id]/` - создать API endpoints
- `messages/*.json` - локализация

### Приоритет 3: Расширение функционала Staff Management

#### 3.1 Добавить возможность присваивать роли
**Текущая структура БД**: 
- Таблица `users` имеет поле `role` (superadmin, staff, participant)
- Таблица `staff_tournament_access` связывает пользователей с турнирами

**Требуется**: Расширить систему ролей и доступов  
**Задачи**:
- [ ] Обновить компонент `AdminStaffContent.tsx`
- [ ] Добавить выбор роли из списка:
  - `superadmin` - Суперадмин (полный доступ ко всему)
  - `tournament_admin` - Админ конкретного турнира (выбор турниров)
  - `manager` - Менеджер (только результаты, без генерации расписания)
- [ ] Обновить форму создания/редактирования доступа
- [ ] Обновить API endpoint `POST /api/admin/staff` и `PUT /api/admin/staff/[id]`
- [ ] Обновить схему БД:
  - Добавить поле `role` в таблицу `staff_tournament_access` (или использовать `users.role`)
  - Добавить поле `permissions` (JSON) для дополнительных разрешений
- [ ] Добавить валидацию (нельзя удалить последнего суперадмина)
- [ ] Обновить логику проверки прав доступа

**Файлы для изменения**:
- `components/pages/AdminStaffContent.tsx`
- `app/api/admin/staff/route.ts`
- `lib/db.ts` - обновить схему БД
- `lib/users.ts` - добавить функции проверки прав
- `messages/*.json` - локализация

**Новая структура данных**:
```typescript
interface StaffAccess {
  id: number;
  userId: string;
  role: 'superadmin' | 'tournament_admin' | 'manager';
  tournamentIds?: number[]; // Для tournament_admin
  permissions?: {
    manageUsers?: boolean;
    manageLogs?: boolean;
    manageTournaments?: boolean;
    canGenerateSchedule?: boolean; // Для manager = false
  };
  // Существующие поля
  canManageGroups: boolean;
  canManageMatches: boolean;
  canViewRegistrations: boolean;
}
```

#### 3.2 Выбор турниров для Tournament Admin
**Текущая структура**: Таблица `staff_tournament_access` уже существует  
**Требуется**: Добавить возможность выбора нескольких турниров  
**Задачи**:
- [ ] Добавить мультиселект для выбора турниров в форме
- [ ] Опция "Выбрать все турниры" (чекбокс)
- [ ] Визуальное отображение выбранных турниров
- [ ] Обновить API для сохранения множественных записей в `staff_tournament_access`
- [ ] Обновить логику отображения (показывать все турниры пользователя)

**Файлы для изменения**:
- `components/pages/AdminStaffContent.tsx`
- `app/api/admin/staff/route.ts`
- `lib/db.ts` - использовать существующую таблицу `staff_tournament_access`

#### 3.3 Дополнительные разрешения (Permissions)
**Текущая структура**: В `staff_tournament_access` есть поля `can_manage_groups`, `can_manage_matches`, `can_view_registrations`  
**Требуется**: Добавить дополнительные разрешения  
**Задачи**:
- [ ] Добавить чекбоксы для дополнительных разрешений:
  - `Manage Users` - Управление пользователями (`/admin/users`)
  - `Manage Logs` - Просмотр логов (`/admin/logs`)
  - `Manage Tournaments` - Редактирование турниров (название, даты, характеристики) (`/admin/tournaments`)
- [ ] Обновить схему БД:
  - Добавить поля в `staff_tournament_access`:
    - `can_manage_users BOOLEAN DEFAULT FALSE`
    - `can_manage_logs BOOLEAN DEFAULT FALSE`
    - `can_manage_tournaments BOOLEAN DEFAULT FALSE`
  - Или использовать JSON поле `permissions`
- [ ] Обновить логику проверки прав доступа в API endpoints
- [ ] Обновить UI для отображения разрешений
- [ ] Добавить проверку прав в компонентах админ-панели

**Файлы для изменения**:
- `components/pages/AdminStaffContent.tsx`
- `app/api/admin/staff/route.ts`
- `app/api/admin/users/route.ts` - добавить проверку `can_manage_users`
- `app/api/admin/logs/route.ts` - добавить проверку `can_manage_logs`
- `app/api/admin/tournaments/route.ts` - добавить проверку `can_manage_tournaments`
- `lib/db.ts` - обновить схему
- `lib/users.ts` - добавить функции проверки прав
- `messages/*.json` - локализация

### Приоритет 4: Новые функции

#### 4.1 Раздел Wallet для СуперАдмина
**Требуется**: Добавить раздел Wallet с подразделами:
- `Wallets` - балансы каждого пользователя (customer wallets)
- `Transactions` - все транзакции

**Задачи**:
- [ ] Создать таблицы в БД:
  - `user_wallets` - кошельки пользователей:
    - `id INT PRIMARY KEY AUTO_INCREMENT`
    - `user_id INT NOT NULL`
    - `balance DECIMAL(10,2) DEFAULT 0.00`
    - `currency VARCHAR(3) DEFAULT 'EUR'`
    - `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP`
    - `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
  - `transactions` - транзакции:
    - `id INT PRIMARY KEY AUTO_INCREMENT`
    - `wallet_id INT NOT NULL`
    - `user_id INT NOT NULL`
    - `type ENUM('deposit', 'withdrawal', 'payment', 'refund', 'bonus')`
    - `amount DECIMAL(10,2) NOT NULL`
    - `description TEXT`
    - `status ENUM('pending', 'completed', 'failed', 'cancelled')`
    - `reference_id VARCHAR(255)` - для связи с внешними системами
    - `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP`
- [ ] Создать страницу `/admin/wallet`:
  - Табы: "Wallets" и "Transactions"
  - Таблица кошельков с балансами
  - Таблица транзакций с фильтрацией
- [ ] Создать компоненты:
  - `components/pages/AdminWalletContent.tsx` - основной компонент
  - `components/pages/wallet/WalletsList.tsx` - список кошельков
  - `components/pages/wallet/TransactionsList.tsx` - список транзакций
- [ ] Создать API endpoints:
  - `GET /api/admin/wallet/wallets` - список кошельков
  - `GET /api/admin/wallet/transactions` - список транзакций
  - `POST /api/admin/wallet/transactions` - создание транзакции
  - `PUT /api/admin/wallet/wallets/[id]` - обновление баланса
- [ ] Добавить локализацию для всех языков
- [ ] Добавить права доступа (только superadmin)

**Файлы для создания/изменения**:
- `app/[locale]/admin/wallet/page.tsx` - страница Wallet
- `components/pages/AdminWalletContent.tsx` - основной компонент
- `components/pages/wallet/WalletsList.tsx` - список кошельков
- `components/pages/wallet/TransactionsList.tsx` - список транзакций
- `app/api/admin/wallet/` - API endpoints
- `lib/db.ts` - создание таблиц
- `messages/*.json` - локализация

**Структура данных**:
```typescript
interface UserWallet {
  id: number;
  userId: number;
  balance: number;
  currency: string;
  createdAt: Date;
  updatedAt: Date;
}

interface Transaction {
  id: number;
  walletId: number;
  userId: number;
  type: 'deposit' | 'withdrawal' | 'payment' | 'refund' | 'bonus';
  amount: number;
  description: string;
  status: 'pending' | 'completed' | 'failed' | 'cancelled';
  referenceId?: string;
  createdAt: Date;
}
```

---

## 🔍 Диагностика проблем

### Проверка подключений к БД
```sql
-- Проверить активные соединения
SHOW PROCESSLIST;

-- Проверить лимиты
SHOW VARIABLES LIKE 'max_connections';
SHOW VARIABLES LIKE 'max_user_connections';
```

### Проверка сессий
- Проверить таблицу `sessions` в БД
- Проверить срок действия токенов
- Проверить логи при входе/выходе

### Проверка логов
- Проверить таблицу `audit_logs` в БД
- Проверить консоль сервера при генерации расписания
- Проверить Network tab в браузере при запросах

---

## 📌 Важные заметки

1. **База данных**: Используется MySQL на `foldis00.mysql.tools`
2. **Админ по умолчанию**: `admin@padelo2.com` / `admin123`
3. **Создание админа**: Использовать `npm run create-admin-direct` (после закрытия всех соединений)
4. **Локализация**: Все тексты должны быть переведены на 14 языков
5. **Логирование**: Все действия пользователей логируются в `audit_logs`

---

## 🚀 Следующие шаги

### Немедленные действия (критические проблемы):
1. **Оптимизация производительности** - профилирование и оптимизация запросов к БД
2. **Исправление потери ID пар** - критическая ошибка, влияет на работу турниров
3. **Диагностика проблем БД** - проверить активные соединения и утечки
4. **Исправление проблемы с сессиями админа**

### Краткосрочные задачи (1-2 недели):
5. ✅ **Разобраться с генерацией расписания** - **ВЫПОЛНЕНО**
   - Исправлена обработка матчей с NULL временем
   - Исправлена установка времени при генерации
   - Матчи финального этапа теперь отображаются в календаре и списке
6. Исправить отображение Audit Logs
7. Реализовать улучшения UX (Login → Account, Dashboard в меню)
8. Добавить детальную информацию о турнире (адрес, Google Maps, расписание событий)
9. Улучшить редактирование пар в Tournament Bracket
10. ✅ **Исправлено дублирование кнопки "Приховати розклад"** - **ВЫПОЛНЕНО**
11. ✅ **Исправлен конфликт маршрутов API [id] и [userId]** - **ВЫПОЛНЕНО**
    - Все подмаршруты перемещены в `[id]`
    - Параметры обновлены с `userId` на `id`

### Среднесрочные задачи (2-4 недели):
10. ✅ **Добавить вкладки в разделе Users** (bookings, matches, wallet, events, trainings) - **РЕАЛИЗОВАНО**
    - API endpoints для всех вкладок созданы
    - Компонент обновлен для отображения реальных данных
    - Локализации добавлены для всех языков
11. ✅ **Реализовать раздел Wallet для СуперАдмина** - **РЕАЛИЗОВАНО**
    - Таблицы `user_wallets` и `wallet_transactions` созданы
    - Компонент `AdminWalletContent.tsx` создан
    - API endpoints `/api/admin/wallet/wallets` и `/api/admin/wallet/transactions` созданы
    - Страница `/admin/wallet` создана
    - Локализации добавлены для всех языков
12. ✅ **Расширить функционал Staff Management** - **РЕАЛИЗОВАНО**
    - Добавлена фильтрация и поиск по списку доступов
    - Добавлена статистика (общее количество доступов, уникальные пользователи, уникальные турниры)
    - Добавлена функция копирования прав доступа от одного пользователя к другому
    - Улучшена визуализация прав доступа
    - Массовое назначение доступа к нескольким турнирам (уже было реализовано)
    - Локализации добавлены для всех языков

---

## 📊 Структура базы данных

### Основные таблицы
- `users` - Пользователи (id, email, password_hash, first_name, last_name, role, created_at)
- `sessions` - Сессии (token, user_id, expires_at)
- `tournaments` - Турниры (id, name, description, start_date, end_date, status, rules, available_courts)
- `tournament_groups` - Группы турнира (id, tournament_id, category, group_name, group_number, max_pairs)
- `tournament_group_pairs` - Пары в группах (id, group_id, pair_number, player1_registration_id, partner1_registration_id, player2_registration_id, partner2_registration_id)
- `tournament_registrations` - Регистрации на турниры (id, tournament_id, user_id, category, first_name, last_name, partner_name, confirmed)
- `tournament_matches` - Матчи (id, group_id, pair1_id, pair2_id, match_date, court_number, pair1_games, pair2_games, winner_pair_id, duration_slots)
- `staff_tournament_access` - Доступ персонала к турнирам (id, user_id, tournament_id, can_manage_groups, can_manage_matches, can_view_registrations)
- `audit_logs` - Логи действий (id, user_id, user_email, user_role, action, entity_type, entity_id, details, ip_address, user_agent, created_at)
- `user_wallets` - Кошельки пользователей (id, user_id, balance, currency, created_at, updated_at) - **✅ РЕАЛИЗОВАНО**
- `wallet_transactions` - Транзакции (id, wallet_id, user_id, type, amount, currency, description, reference_type, reference_id, status, created_by, created_at, updated_at) - **✅ РЕАЛИЗОВАНО**

### Текущие настройки пула соединений
- `connectionLimit: 3` (в `lib/db.ts`)
- `connectTimeout: 10000` (10 секунд)
- `waitForConnections: true`
- `queueLimit: 0`

---

## 🔑 Учетные данные

**Super Admin по умолчанию**:
- Email: `admin@padelo2.com`
- Password: `admin123`

**Создание админа**:
```bash
npm run create-admin-direct
```
*Примечание: Перед выполнением закрыть все активные соединения к БД*

---

## 📝 Примечания для разработчика

1. **Локализация**: Все новые тексты должны быть переведены на все 14 языков
2. **Логирование**: Все действия пользователей должны логироваться через `logAction()`
3. **Права доступа**: Проверять права доступа во всех API endpoints
4. **Оптимистичные обновления**: Использовать для улучшения UX (drag & drop, редактирование)
5. **Обработка ошибок**: Всегда возвращать понятные сообщения об ошибках
6. **Типы**: Использовать TypeScript типы для всех интерфейсов

---

## 🎯 Следующий план реализации

### Приоритет 1: Критические проблемы (немедленно)

#### 1. Оптимизация производительности БД
**Почему важно**: Медленная работа сайта влияет на пользовательский опыт  
**Задачи**:
- [ ] Провести профилирование всех запросов к БД
- [ ] Добавить индексы на часто используемые поля (см. раздел 1.5)
- [ ] Оптимизировать N+1 запросы
- [ ] Добавить кэширование для часто запрашиваемых данных
- [ ] Использовать `Promise.all()` для параллельных запросов

**Ожидаемый результат**: Уменьшение времени загрузки страниц в 2-3 раза

#### 2. Исправление потери ID пар при перемещении
**Почему важно**: Критическая ошибка, влияет на работу турниров  
**Задачи**:
- [ ] Исправить логику перемещения пар в `TournamentBracket.tsx`
- [ ] Сохранять уникальный `pair_id` при перемещении между группами
- [ ] Добавить проверку уникальности `pair_number` в группе
- [ ] Обновить визуальное отображение

**Ожидаемый результат**: Пары сохраняют уникальные ID при перемещении

#### 3. Диагностика проблем БД
**Почему важно**: Превышение лимита соединений блокирует работу  
**Задачи**:
- [ ] Проверить все места открытия соединений
- [ ] Убедиться, что все соединения закрываются
- [ ] Добавить мониторинг активных соединений
- [ ] Проверить утечки в API endpoints

**Ожидаемый результат**: Стабильная работа без ошибок соединений

#### 4. Исправление проблемы с сессиями админа
**Почему важно**: Админ не может работать, если его выкидывает из системы  
**Задачи**:
- [ ] Проверить логику создания/проверки сессий
- [ ] Проверить срок действия токенов
- [ ] Добавить логирование при проверке сессий
- [ ] Проверить middleware

**Ожидаемый результат**: Стабильные сессии для админа

### Приоритет 2: Улучшения UX (1-2 недели)

#### 1. Audit Logs - исправление отображения
**Задачи**:
- [ ] Проверить создание таблицы `audit_logs`
- [ ] Проверить API endpoint и компонент
- [ ] Добавить логирование ошибок
- [ ] Проверить вызовы `logAction()`

#### 2. Улучшения навигации
- [ ] Login → Account (изменить текст)
- [ ] Добавить Dashboard в меню для авторизованных

#### 3. Детальная информация о турнире
- [ ] Адрес с Google Maps
- [ ] Расписание событий (лотерея, автограф-сессии)
- [ ] Редактирование для админов

#### 4. Улучшение редактирования пар
- [ ] Прямое редактирование имен игроков
- [ ] Добавление новых игроков
- [ ] Уведомления через WhatsApp и Email

### Приоритет 3: Наполнение контентом

#### 1. Загрузка изображений для главной страницы
**Статус**: Создан чеклист `IMAGES_CHECKLIST.md`  
**Задачи**:
- [ ] Загрузить все 33 изображения согласно чеклисту
- [ ] Загрузить видео `hero-bg.mp4`
- [ ] Загрузить логотипы (3 файла)

**Ожидаемый результат**: Полностью наполненная главная страница

### Приоритет 4: Новые функции (2-4 недели)

#### 1. Расширение функционала Staff Management
- [ ] Добавить роли (tournament_admin, manager)
- [ ] Выбор турниров для Tournament Admin
- [ ] Дополнительные разрешения (permissions)

#### 2. Улучшения турниров
- [ ] Уведомления участникам
- [ ] Экспорт результатов
- [ ] Статистика турниров

---

*Документ создан: 2025-11-14*  
*Последнее обновление: 2025-11-15 - обновлен статус задач: раздел Wallet, вкладки в Users и расширение Staff Management реализованы*  
*Обновление: 2025-11-15 (вечер) - исправлены проблемы с кнопкой "Приховати розклад", генерацией расписания для финального этапа, конфликт маршрутов API*

