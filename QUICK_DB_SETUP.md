# ⚡ Быстрая настройка подключения к БД

## 📝 Шаг 1: Создайте файл .env.local

В корне проекта создайте файл `.env.local` со следующим содержимым:

```env
# Database Configuration
DATABASE_HOST=ваш_хост_здесь
DATABASE_PORT=3306
DATABASE_NAME=ваше_имя_бд
DATABASE_USER=ваш_пользователь
DATABASE_PASSWORD=ваш_пароль

# Site URL
NEXT_PUBLIC_SITE_URL=https://padelo2.com
```

**Пример с реальными данными (из DATABASE_SETUP_UKRAINE.md):**

```env
DATABASE_HOST=foldis00.mysql.tools
DATABASE_PORT=3306
DATABASE_NAME=foldis00_padelo2
DATABASE_USER=foldis00_padelo2
DATABASE_PASSWORD=6p^XZbu!34

NEXT_PUBLIC_SITE_URL=https://padelo2.com
```

---

## ✅ Шаг 2: Проверьте подключение

Запустите скрипт для проверки:

```bash
npx tsx scripts/test-db-connection.ts
```

Если всё правильно, вы увидите:
```
✅ Database connection test completed successfully!
```

---

## 🚀 Шаг 3: Перезапустите сервер

```bash
npm run dev
```

В консоли должно появиться:
```
🔌 Connecting to database: ваш_хост:3306/ваша_бд
✅ Database and admin initialization completed
```

---

## 📋 Что нужно от вас:

1. **Host БД** (например: `foldis00.mysql.tools` или `localhost`)
2. **Имя БД** (например: `foldis00_padelo2`)
3. **Пользователь БД** (например: `foldis00_padelo2`)
4. **Пароль БД**

После получения этих данных:
- Создайте `.env.local` с этими данными
- Запустите тест подключения
- Перезапустите сервер

---

## ⚠️ Важно:

- Файл `.env.local` уже в `.gitignore` - он не будет закоммичен
- Используйте **те же данные** на локальной машине и на сервере
- После настройки локальная и серверная версии будут использовать **одну БД**

---

## 🐛 Проблемы?

Смотрите подробную инструкцию: `DATABASE_CONNECTION.md`

