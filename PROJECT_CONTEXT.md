# 📋 Контекст проекта PadelO₂

> **Последнее обновление:** Декабрь 2024  
> **Статус проекта:** В разработке (MVP готов, требуется доработка для production)

## 📊 Краткое резюме

**Реализовано:**
- ✅ Система регистрации на турниры (полностью функциональна)
- ✅ База данных MySQL с таблицей регистраций
- ✅ Email отправка (Resend/SendGrid/SMTP)
- ✅ Аутентификация и авторизация (базовая, in-memory)
- ✅ Админ-панель (UI готов, API не реализован)
- ✅ Dashboard участника (UI готов, загрузка фото не реализована)
- ✅ Система тем (dark/light)
- ✅ Многоязычность (14 языков)

**Требует доработки для production:**
- ❌ Миграция аутентификации на БД (сейчас in-memory)
- ❌ Хеширование паролей (bcrypt)
- ❌ API endpoints для админ-панели
- ❌ Безопасность (rate limiting, CSRF, JWT)
- ❌ Загрузка фото участника
- ❌ Интеграция контактной формы с email
- ❌ Форма инвестиций

## 🎯 Общее описание проекта

**PadelO₂** — многоязычный веб-сайт для экосистемы падел-спорта с поддержкой 14 языков:
- English (en), Spanish (es), Ukrainian (ua), Russian (ru), Catalan (ca)
- Chinese (zh), Dutch (nl), Danish (da), Swedish (sv), German (de)
- Norwegian (no), Italian (it), French (fr), Arabic (ar)

## 🛠 Технологический стек

- **Framework:** Next.js 14 (App Router)
- **Language:** TypeScript
- **Styling:** TailwindCSS
- **Animations:** Framer Motion
- **Internationalization:** next-intl
- **Database:** MySQL (mysql2/promise)
- **Email:** Resend/SendGrid/SMTP (поддержка всех трех)

### Зависимости (package.json):
**Production:**
- `next: ^14.2.0`
- `react: ^18.3.0`, `react-dom: ^18.3.0`
- `next-intl: ^3.15.0`
- `framer-motion: ^11.0.0`
- `react-intersection-observer: ^9.5.0`
- `mysql2: ^3.6.5`
- `resend: ^3.2.0`
- `@sendgrid/mail: ^8.1.0`
- `nodemailer: ^6.9.8`

**Development:**
- `typescript: ^5.3.0`
- `tailwindcss: ^3.4.0`
- `eslint: ^8.56.0`

## 📁 Структура проекта

### Основные компоненты
- `components/sections/` — секции главной страницы (Hero, Courts, Machines, etc.)
- `components/pages/` — контент страниц (TournamentsContent, DashboardContent, etc.)
- `components/forms/` — формы (TournamentRegistrationForm, ContactForm)
- `components/modals/` — модальные окна (InvestmentModal)
- `components/providers/` — провайдеры (ClientProviders)
- `app/[locale]/` — локализованные страницы
- `app/api/` — API роуты
- `lib/` — утилиты (db.ts, auth.ts, tournament-storage.ts)
- `messages/` — файлы переводов (14 языков)
- `contexts/` — React контексты (ThemeContext)

### API Endpoints
**Реализованные:**
- `POST /api/tournament/register` — регистрация на турнир
- `GET /api/tournament/register?token=...` — получение регистрации по токену
- `POST /api/tournament/confirm` — подтверждение регистрации
- `POST /api/auth/login` — вход в систему
- `GET /api/auth/login` — проверка сессии
- `POST /api/auth/register` — регистрация пользователя
- `POST /api/contact` — отправка контактной формы (без email)

**Не реализованные:**
- `GET /api/admin/users` — список пользователей
- `POST /api/admin/users` — создание пользователя
- `PUT /api/admin/users/:id` — обновление пользователя
- `DELETE /api/admin/users/:id` — удаление пользователя
- `GET /api/admin/tournaments` — список турниров
- `POST /api/admin/tournaments` — создание турнира
- `PUT /api/admin/tournaments/:id` — обновление турнира
- `DELETE /api/admin/tournaments/:id` — удаление турнира
- `POST /api/tournament/upload-photo` — загрузка фото участника
- `POST /api/investments` — отправка формы инвестиций

## ✅ Реализованные функции

### 1. Система регистрации на турниры
- ✅ Форма регистрации (`TournamentRegistrationForm.tsx`)
- ✅ API для регистрации (`/api/tournament/register`)
- ✅ API для подтверждения (`/api/tournament/confirm`)
- ✅ Хранение данных (in-memory для dev, MySQL для production)
- ✅ Отправка email с подтверждением
- ✅ Страница подтверждения (`/tournament/confirmation`)
- ✅ Dashboard участника (`/tournament/dashboard`)

**Функционал формы:**
- Регистрация участника (имя, email, телефон, telegram)
- Выбор категорий (male1, male2, female1, female2, mixed1, mixed2)
- Логика выбора категорий:
  - Можно выбрать только одну male категорию (male1 ИЛИ male2)
  - Можно выбрать только одну female категорию (female1 ИЛИ female2)
  - Можно выбрать только одну mixed категорию (mixed1 ИЛИ mixed2)
  - Можно выбрать male + mixed ИЛИ female + mixed, но НЕ male + female одновременно
- Добавление партнера (опционально)
- Загрузка фото партнера
- Выбор размера футболки

### 2. База данных
- ✅ Подключение к MySQL (`lib/db.ts`)
- ✅ Таблица `tournament_registrations` с полной структурой
- ✅ Автоматическое создание таблицы при инициализации
- ✅ Fallback на in-memory хранилище если БД не настроена

**Структура таблицы:**
```sql
tournament_registrations:
- id, token, tournament_id, tournament_name, locale
- first_name, last_name, email, telegram, phone
- categories (JSON), tshirt_size, message
- partner_name, partner_email, partner_phone, partner_tshirt_size
- partner_photo_name, partner_photo_data (LONGTEXT)
- confirmed, created_at, confirmed_at
```

### 3. Аутентификация и авторизация
- ✅ Система ролей (`lib/auth.ts`):
  - `superadmin` — полный доступ
  - `staff` — управление турнирами и регистрациями
  - `participant` — доступ к своему dashboard
- ✅ API для авторизации (`/api/auth/login`)
  - POST `/api/auth/login` — вход в систему
  - GET `/api/auth/login` — проверка сессии по токену
- ✅ API для регистрации (`/api/auth/register`)
- ✅ Проверка прав доступа (`canAccessRoute`, `hasPermission`)
- ✅ Страница логина (`/login`)
- ⚠️ **Ограничения:** 
  - Пользователи хранятся в памяти (in-memory), не в БД
  - Пароли не хешируются (только для dev)
  - Дефолтный superadmin: `admin@padelo2.com` / `admin123`

### 4. Админ-панель
- ✅ Структура админ-панели (`/admin/`)
  - `/admin/users` — управление пользователями
  - `/admin/tournaments` — управление турнирами
  - `/admin/staff` — управление персоналом
- ✅ Компоненты UI:
  - `AdminUsersContent.tsx` — интерфейс управления пользователями
  - `AdminTournamentsContent.tsx` — интерфейс управления турнирами
  - `AdminStaffContent.tsx` — интерфейс управления персоналом
- ✅ Dashboard для разных ролей (`/dashboard`)
  - `DashboardPageContent.tsx` — главный dashboard с навигацией
  - Разные представления для superadmin, staff, participant
- ❌ **Не реализовано:** API endpoints для CRUD операций

### 5. Email отправка
- ✅ Поддержка трех провайдеров:
  - Resend (приоритет) — `resend` пакет
  - SendGrid — `@sendgrid/mail` пакет
  - SMTP (через nodemailer) — `nodemailer` пакет
- ✅ Отправка email с подтверждением регистрации на турниры
- ✅ HTML шаблон письма с ссылкой для подтверждения
- ✅ Fallback на console.log если email не настроен
- ✅ Многоязычные письма (поддержка локализации)

### 6. Система тем
- ✅ `ThemeContext` — контекст для управления темой
- ✅ `ThemeToggle.tsx` — компонент переключения темы
- ✅ Сохранение выбора темы в localStorage
- ✅ Поддержка dark/light режимов
- ✅ Интеграция с TailwindCSS через data-theme атрибут

### 7. Многоязычность
- ✅ 14 языков поддержки
- ✅ next-intl для интернационализации
- ✅ Переводы для всех компонентов
- ✅ Локализованные URL (`/[locale]/...`)
- ✅ Автоматическое определение языка

## 🚧 Незавершенные задачи (TODO)

### 1. Админ-панель — API endpoints
**Приоритет: ВЫСОКИЙ**

#### 1.1. Управление пользователями
**Файл:** `components/pages/AdminUsersContent.tsx` (строка 37)
- ❌ TODO: Fetch users from API
- ❌ Нужно создать API endpoint `/api/admin/users`:
  - GET — получение списка пользователей
  - POST — создание нового пользователя
  - PUT — обновление пользователя
  - DELETE — удаление пользователя
- ❌ Нужно создать таблицу `users` в БД:
  ```sql
  users:
  - id, email, password_hash, first_name, last_name
  - role, created_at, updated_at
  ```
- ❌ Реализовать хеширование паролей (bcrypt)

#### 1.2. Управление турнирами
**Файл:** `components/pages/AdminTournamentsContent.tsx`
- ❌ Нужно создать API endpoint `/api/admin/tournaments`:
  - GET — список турниров
  - POST — создание турнира
  - PUT — обновление турнира
  - DELETE — удаление турнира
- ❌ Нужно создать таблицу `tournaments` в БД

#### 1.3. Управление персоналом
**Файл:** `components/pages/AdminStaffContent.tsx`
- ❌ Нужно создать API endpoint `/api/admin/staff`
- ❌ Реализовать назначение ролей staff пользователям

### 2. Контактная форма
**Файл:** `app/api/contact/route.ts` (строка 18)
**Приоритет: СРЕДНИЙ**
- ❌ TODO: Интегрировать с email сервисом
- ❌ Нужно добавить отправку email при отправке контактной формы
- ❌ Использовать существующую инфраструктуру email (Resend/SendGrid/SMTP)
- ❌ Добавить уведомление администратору о новой заявке

### 3. Модальное окно инвестиций
**Файл:** `components/modals/InvestmentModal.tsx` (строка 25)
**Приоритет: СРЕДНИЙ**
- ❌ TODO: Implement form submission
- ❌ Нужно создать API endpoint `/api/investments` для отправки формы
- ❌ Нужно создать таблицу `investment_requests` в БД:
  ```sql
  investment_requests:
  - id, name, email, company, investment_size
  - message, created_at, status
  ```
- ❌ Добавить отправку email администратору

### 4. Dashboard участника — загрузка фото
**Файл:** `components/pages/DashboardContent.tsx` (строка 152-177)
**Приоритет: СРЕДНИЙ**
- ❌ Загрузка фото участника не реализована (только UI)
- ❌ Нужно добавить API endpoint `/api/tournament/upload-photo`:
  - POST — загрузка фото участника
  - Валидация размера (макс. 5MB)
  - Валидация формата (jpg, png, webp)
- ❌ Нужно добавить поле `participant_photo` в таблицу `tournament_registrations`
- ❌ Сохранение фото в БД (LONGTEXT) или файловую систему

### 5. База данных — production настройка
**Файл:** `DATABASE_SETUP_UKRAINE.md`
**Приоритет: ВЫСОКИЙ**
- ⚠️ Нужны данные для подключения к БД на хостинге Ukraine.com.ua
- ⚠️ Нужно создать таблицу `tournament_registrations` на production сервере
- ⚠️ Нужно создать таблицы для админки:
  - `users` — пользователи системы
  - `tournaments` — турниры
  - `investment_requests` — заявки на инвестиции
- ⚠️ Нужно настроить переменные окружения в `.env.local` на production

### 6. Безопасность
**Приоритет: ВЫСОКИЙ**
- ❌ Хеширование паролей (bcrypt) — сейчас пароли хранятся в открытом виде
- ❌ Rate limiting для API endpoints
- ❌ CSRF защита для форм
- ❌ Валидация и санитизация всех входных данных
- ❌ JWT токены вместо простых session токенов
- ❌ HTTPS только для production

### 7. Аутентификация — миграция на БД
**Файл:** `app/api/auth/login/route.ts`
**Приоритет: ВЫСОКИЙ**
- ⚠️ Сейчас пользователи хранятся в памяти (Map)
- ❌ Нужно мигрировать на MySQL таблицу `users`
- ❌ Реализовать хеширование паролей
- ❌ Добавить систему восстановления пароля

## 📝 Текущее состояние Git

### Staged (готово к коммиту):
- ✅ Новая система регистрации на турниры
- ✅ API endpoints для турниров (`/api/tournament/register`, `/api/tournament/confirm`)
- ✅ Компоненты Dashboard и Confirmation
- ✅ Обновления переводов для всех языков
- ✅ Скрипт обновления переводов (`update_tournament_translations.sh`)

### Modified (не staged):
- `app/[locale]/layout.tsx` — добавлена интеграция ThemeProvider
- `app/api/tournament/confirm/route.ts` — доработки логики подтверждения
- `app/api/tournament/register/route.ts` — доработки регистрации
- `components/forms/TournamentRegistrationForm.tsx` — улучшения формы
- `components/pages/DashboardContent.tsx` — доработки dashboard
- `components/pages/ConfirmationContent.tsx` — доработки страницы подтверждения
- `components/pages/TournamentsContent.tsx` — интеграция формы регистрации
- `lib/tournament-storage.ts` — доработки хранения данных
- Все файлы переводов (`messages/*.json`) — обновления переводов
- `package.json`, `package-lock.json` — новые зависимости
- `tailwind.config.ts` — настройки темы
- `app/globals.css` — стили для темы
- `components/Header.tsx` — интеграция темы
- `components/LanguageSelector.tsx` — доработки

### Untracked (новые файлы, не добавлены в git):
- `app/[locale]/admin/` — админ-панель (3 страницы)
- `app/[locale]/dashboard/` — dashboard участника
- `app/[locale]/login/` — страница логина
- `app/api/auth/` — API авторизации (login, register)
- `lib/auth.ts` — система авторизации и ролей
- `lib/db.ts` — подключение к MySQL
- `components/pages/AdminUsersContent.tsx` — управление пользователями
- `components/pages/AdminTournamentsContent.tsx` — управление турнирами
- `components/pages/AdminStaffContent.tsx` — управление персоналом
- `components/pages/LoginContent.tsx` — форма логина
- `components/pages/DashboardPageContent.tsx` — главный dashboard
- `components/ThemeToggle.tsx` — переключатель темы
- `contexts/ThemeContext.tsx` — контекст темы
- `components/providers/ClientProviders.tsx` — провайдеры для клиента
- `public/images/tournaments/` — изображения турниров
- Документация: `DATABASE_SETUP_UKRAINE.md`, `FIX_WHITE_SCREEN.md`, `PROJECT_CONTEXT.md`, `QUICK_FIX_WHITE_SCREEN.md`

## 🔧 Настройка окружения

### Переменные окружения (`.env.local`):
```bash
# Database Configuration
DATABASE_HOST=localhost
DATABASE_NAME=foldis00_padelo2
DATABASE_USER=foldis00_padelo2
DATABASE_PASSWORD=ваш_пароль
DATABASE_PORT=3306

# Site URL
NEXT_PUBLIC_SITE_URL=https://padelo2.com

# Email Configuration (выберите один)
RESEND_API_KEY=re_...
# или
SENDGRID_API_KEY=SG....
# или
SMTP_HOST=smtp.ukraine.com.ua
SMTP_PORT=587
SMTP_USER=noreply@padelo2.com
SMTP_PASSWORD=ваш_пароль
SMTP_FROM=noreply@padelo2.com
```

## 🎨 Особенности UI/UX

### Темная/светлая тема
- ✅ Реализована через `ThemeContext`
- ✅ Компонент `ThemeToggle.tsx`
- ✅ Сохранение выбора темы в localStorage

### Анимации
- ✅ Framer Motion для плавных переходов
- ✅ Анимации в Hero секции
- ✅ Scroll indicator

### Адаптивность
- ✅ Responsive дизайн для всех компонентов
- ✅ Mobile-first подход

## 📊 Логика выбора категорий турнира

Реализована сложная логика выбора категорий:
1. **Male категории:** можно выбрать только одну (male1 ИЛИ male2)
2. **Female категории:** можно выбрать только одну (female1 ИЛИ female2)
3. **Mixed категории:** можно выбрать только одну (mixed1 ИЛИ mixed2)
4. **Комбинации:**
   - ✅ male + mixed (разрешено)
   - ✅ female + mixed (разрешено)
   - ❌ male + female (запрещено)
   - ❌ male + female + mixed (запрещено)

## 🔐 Безопасность

**Реализовано:**
- ✅ Токены для подтверждения регистрации (crypto.randomBytes, 64 символа)
- ✅ Проверка прав доступа для админ-панели (`canAccessRoute`, `hasPermission`)
- ✅ Валидация данных на клиенте и сервере
- ✅ Защита маршрутов админки (проверка роли)

**Не реализовано (критично для production):**
- ❌ Хеширование паролей (bcrypt) — пароли хранятся в открытом виде
- ❌ Rate limiting для API endpoints
- ❌ CSRF защита для форм
- ❌ JWT токены вместо простых session токенов
- ❌ Валидация и санитизация всех входных данных (нужно усилить)
- ❌ HTTPS только для production
- ❌ Защита от SQL injection (используется параметризованные запросы, но нужно проверить все места)

## 📧 Email функционал

Реализована отправка email с подтверждением регистрации:
- Поддержка трех провайдеров (Resend, SendGrid, SMTP)
- HTML шаблон письма
- Ссылка для подтверждения регистрации
- Fallback на console.log в development

## 🚀 Следующие шаги (приоритеты)

### Критично для production (ВЫСОКИЙ приоритет):

1. **Миграция аутентификации на БД:**
   - Создать таблицу `users` в MySQL
   - Реализовать хеширование паролей (bcrypt)
   - Мигрировать код с in-memory на БД
   - Добавить систему восстановления пароля

2. **Завершить админ-панель — API endpoints:**
   - `/api/admin/users` — CRUD для пользователей
   - `/api/admin/tournaments` — CRUD для турниров
   - `/api/admin/staff` — управление персоналом
   - Создать соответствующие таблицы в БД

3. **Безопасность:**
   - Добавить rate limiting
   - Реализовать CSRF защиту
   - Перейти на JWT токены
   - Усилить валидацию входных данных

4. **Настроить production БД:**
   - Создать все таблицы на хостинге Ukraine.com.ua
   - Настроить переменные окружения
   - Протестировать подключение

### Важно, но не критично (СРЕДНИЙ приоритет):

5. **Реализовать загрузку фото участника:**
   - API endpoint `/api/tournament/upload-photo`
   - Добавить поле в таблицу `tournament_registrations`
   - Валидация размера и формата

6. **Доработать контактную форму:**
   - Интегрировать с email сервисом
   - Добавить уведомления администратору

7. **Реализовать форму инвестиций:**
   - API endpoint `/api/investments`
   - Создать таблицу `investment_requests`
   - Отправка email администратору

### Улучшения (НИЗКИЙ приоритет):

8. **Оптимизация:**
   - Кэширование данных
   - Оптимизация запросов к БД
   - Lazy loading компонентов

9. **Дополнительные функции:**
   - Экспорт данных турниров
   - Статистика регистраций
   - Уведомления участникам

## 📌 Важные файлы для продолжения работы

### Основные утилиты:
- `lib/tournament-storage.ts` — логика хранения регистраций (saveRegistration, getRegistration, confirmRegistration)
- `lib/db.ts` — подключение к MySQL (getDbPool, initDatabase)
- `lib/auth.ts` — система авторизации (роли, права доступа, canAccessRoute, hasPermission)

### API Routes:
- `app/api/tournament/register/route.ts` — регистрация на турнир
- `app/api/tournament/confirm/route.ts` — подтверждение регистрации
- `app/api/auth/login/route.ts` — авторизация (нужно мигрировать на БД)
- `app/api/auth/register/route.ts` — регистрация пользователя
- `app/api/contact/route.ts` — контактная форма (нужно добавить email)

### Компоненты:
- `components/forms/TournamentRegistrationForm.tsx` — форма регистрации на турнир
- `components/pages/DashboardContent.tsx` — dashboard участника (нужно добавить загрузку фото)
- `components/pages/AdminUsersContent.tsx` — управление пользователями (нужен API)
- `components/pages/AdminTournamentsContent.tsx` — управление турнирами (нужен API)
- `components/pages/AdminStaffContent.tsx` — управление персоналом (нужен API)

### Конфигурация:
- `DATABASE_SETUP_UKRAINE.md` — инструкция по настройке БД на хостинге
- `.env.local` — переменные окружения (нужно создать на production)
- `tailwind.config.ts` — конфигурация TailwindCSS с поддержкой тем
- `next.config.mjs` — конфигурация Next.js

### Документация:
- `PROJECT_CONTEXT.md` — этот файл (контекст проекта)
- `DATABASE_SETUP_UKRAINE.md` — настройка БД
- `FIX_WHITE_SCREEN.md` — исправление белого экрана

