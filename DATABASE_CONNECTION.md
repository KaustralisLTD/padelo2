# 🔌 Настройка подключения к общей базе данных

## 📋 Цель

Настроить подключение к одной и той же БД для:
- ✅ Локальной версии (разработка)
- ✅ Серверной версии (production)

Это позволит иметь общие данные между версиями.

---

## 🔧 Шаг 1: Получите данные БД

Вам нужны следующие данные от хостинга:

1. **Host (хост):** `_________________`
   - Обычно: `localhost` или `foldis00.mysql.tools` или `mysql.foldis00.ua`
   
2. **Port (порт):** `_________________`
   - Обычно: `3306`
   
3. **Database name (имя БД):** `_________________`
   - Например: `foldis00_padelo2`
   
4. **Username (пользователь):** `_________________`
   - Например: `foldis00_padelo2`
   
5. **Password (пароль):** `_________________`
   - Пароль, который вы установили или который предоставил хостинг

---

## 🔧 Шаг 2: Создайте файл .env.local

В корне проекта создайте файл `.env.local` (он уже в .gitignore, не будет закоммичен):

```bash
# Скопируйте шаблон
cp .env.local.example .env.local
```

Или создайте вручную файл `.env.local` со следующим содержимым:

```env
# Database Configuration
DATABASE_HOST=ваш_хост_здесь
DATABASE_PORT=3306
DATABASE_NAME=ваше_имя_бд_здесь
DATABASE_USER=ваш_пользователь_здесь
DATABASE_PASSWORD=ваш_пароль_здесь

# Site URL
NEXT_PUBLIC_SITE_URL=https://padelo2.com

# Email (опционально)
RESEND_API_KEY=ваш_api_ключ_здесь
```

**Пример заполненного файла:**

```env
DATABASE_HOST=foldis00.mysql.tools
DATABASE_PORT=3306
DATABASE_NAME=foldis00_padelo2
DATABASE_USER=foldis00_padelo2
DATABASE_PASSWORD=6p^XZbu!34

NEXT_PUBLIC_SITE_URL=https://padelo2.com

RESEND_API_KEY=re_MoaNSNHY_Fs2h8YSh8DyG9odc4b3iQ1Nn
```

---

## 🔧 Шаг 3: Настройка на сервере

На сервере также создайте файл `.env.local` с теми же данными.

**Для FTP-деплоя:**
1. Загрузите `.env.local` на сервер в корень проекта
2. Убедитесь, что файл имеет права доступа 644

**Для Git-деплоя:**
1. Создайте `.env.local` на сервере
2. Или используйте переменные окружения в панели хостинга

---

## ✅ Шаг 4: Проверка подключения

### Локально:

1. Перезапустите dev-сервер:
   ```bash
   npm run dev
   ```

2. Проверьте логи в консоли:
   - ✅ Должно быть: `✅ Database and admin initialization completed`
   - ❌ Если ошибка: проверьте данные в `.env.local`

3. Попробуйте войти:
   - Email: `admin@padelo2.com`
   - Password: `admin123`
   - Админ создастся автоматически в БД

### На сервере:

1. Перезапустите приложение
2. Проверьте логи
3. Попробуйте войти с теми же данными

---

## 🧪 Тестирование подключения

### Вариант 1: Через скрипт

```bash
npx tsx scripts/create-admin.ts
```

Если подключение успешно, вы увидите:
```
✅ Admin user created successfully!
```

### Вариант 2: Через MySQL клиент

```bash
mysql -h ваш_хост -u ваш_пользователь -p ваше_имя_бд
```

Введите пароль и проверьте таблицы:
```sql
SHOW TABLES;
SELECT * FROM users;
```

---

## 🔍 Проверка работы

После настройки:

1. **Локально:** Создайте пользователя через админ-панель
2. **На сервере:** Проверьте, что этот пользователь виден
3. **И наоборот:** Создайте на сервере, проверьте локально

Если данные синхронизируются - всё работает! ✅

---

## ⚠️ Важные моменты

### Безопасность:

1. **НЕ коммитьте** `.env.local` в Git (уже в .gitignore)
2. **НЕ делитесь** паролями в открытом виде
3. Используйте **сильные пароли** для БД
4. Ограничьте доступ к БД только с ваших IP (если возможно)

### Производительность:

1. Удаленное подключение может быть медленнее локального
2. Для разработки можно использовать локальную БД (если есть)
3. Для production обязательно используйте удаленную БД

### Резервное копирование:

1. Регулярно делайте бэкапы БД
2. Храните бэкапы в безопасном месте
3. Тестируйте восстановление из бэкапа

---

## 🐛 Решение проблем

### Ошибка: "Database credentials not configured"

**Причина:** Файл `.env.local` не найден или не заполнен

**Решение:**
1. Проверьте, что файл `.env.local` существует в корне проекта
2. Проверьте, что все переменные заполнены
3. Перезапустите сервер

### Ошибка: "ECONNREFUSED"

**Причина:** Не удается подключиться к БД

**Решение:**
1. Проверьте правильность `DATABASE_HOST`
2. Проверьте, что БД доступна с вашего IP
3. Проверьте файрвол/брандмауэр
4. Убедитесь, что порт 3306 открыт

### Ошибка: "Access denied"

**Причина:** Неправильные логин/пароль

**Решение:**
1. Проверьте `DATABASE_USER` и `DATABASE_PASSWORD`
2. Убедитесь, что пользователь имеет права на БД
3. Проверьте, что имя БД правильное

### Данные не синхронизируются

**Причина:** Разные БД или разные подключения

**Решение:**
1. Убедитесь, что локально и на сервере используются одинаковые данные
2. Проверьте файлы `.env.local` на обеих сторонах
3. Убедитесь, что подключение действительно к одной БД

---

## 📝 Структура таблиц

После подключения будут автоматически созданы таблицы:

- `users` - пользователи системы
- `sessions` - сессии пользователей
- `tournament_registrations` - регистрации на турниры

Если таблицы уже существуют, они не будут пересозданы.

---

## 🚀 Готово!

После настройки:
- ✅ Локальная и серверная версии используют одну БД
- ✅ Данные синхронизируются автоматически
- ✅ Пользователи, турниры, регистрации - всё общее

