# 🚀 Завершение настройки на сервере

## ✅ Файлы уже загружены на FTP!

Теперь нужно настроить Node.js приложение на сервере.

## 📋 Шаги на сервере:

### 1. Войдите в панель управления Ukraine.com.ua

### 2. Активируйте Node.js приложение:

1. Перейдите в раздел **"Node.js"** в панели управления
2. Нажмите **"Создать новое приложение"** или **"Добавить приложение"**
3. Заполните форму:
   - **Название:** `padelo2` (или любое другое)
   - **Версия Node.js:** `18.x` или `20.x` (рекомендуется 20.x)
   - **Рабочая директория:** путь к папке, куда вы загрузили файлы (обычно `/home/username/public_html` или `/home/username/domains/yourdomain.com/public_html`)
   - **Start Command:** `npm start`
   - **Port:** оставьте автоматический (обычно 3000 или другой свободный порт)

### 3. Установите зависимости:

**Через SSH (если есть доступ):**
```bash
cd /path/to/your/app
npm install --production
```

**Или через консоль в панели управления:**
- Найдите раздел "Консоль" или "Terminal"
- Выполните команды выше

### 4. Запустите приложение:

В настройках Node.js приложения:
- Убедитесь, что **Start Command** установлен: `npm start`
- Нажмите **"Запустить"** или **"Start"**

### 5. Настройте домен:

1. В панели управления найдите раздел **"Домены"** или **"DNS"**
2. Убедитесь, что ваш домен указывает на Node.js приложение
3. Хостинг автоматически настроит проксирование на ваш Next.js сервер

## 🔍 Проверка работы:

После запуска проверьте:
- [ ] Сайт открывается по домену: `https://yourdomain.com`
- [ ] Главная страница загружается: `https://yourdomain.com/en`
- [ ] Все языки работают: `/es`, `/ru`, `/ua`, и т.д.
- [ ] Изображения загружаются
- [ ] Формы работают
- [ ] WhatsApp кнопка работает

## 🆘 Если что-то не работает:

### Сайт не открывается:
1. Проверьте логи в панели управления Node.js
2. Убедитесь, что приложение запущено (статус "Running")
3. Проверьте, что порт настроен правильно

### Ошибка "Cannot find module":
```bash
# На сервере выполните:
cd /path/to/your/app
npm install --production
```

### Ошибка "Port already in use":
- Измените порт в настройках Node.js приложения
- Или остановите другое приложение, использующее этот порт

### Изображения не загружаются:
- Убедитесь, что папка `public/` загружена полностью
- Проверьте права доступа к файлам (должны быть 755 для папок, 644 для файлов)

## 📞 Поддержка:

Если возникнут проблемы:
1. Проверьте [документацию Ukraine.com.ua по Next.js](https://www.ukraine.com.ua/wiki/hosting/nodejs/nextjs/)
2. Обратитесь в техподдержку хостинга
3. Проверьте логи приложения в панели управления

## ✅ Готово!

После выполнения всех шагов ваш сайт должен работать на сервере!

