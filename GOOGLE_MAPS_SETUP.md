# 🗺️ Настройка Google Maps API

## Проблема

При отображении адреса турнира появляется ошибка:
```
Google Maps Platform rejected your request. You must use an API key to authenticate each request to Google Maps Platform APIs.
```

## Решение

### Шаг 1: Получите Google Maps API ключ

1. Зайдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **Create Credentials** → **API Key**
5. Скопируйте созданный API ключ

### Шаг 2: Включите необходимые API

В Google Cloud Console → **APIs & Services** → **Library** включите:
- **Maps Embed API** (для встраивания карт)
- **Maps JavaScript API** (для интерактивных карт, если планируете использовать)

### Шаг 3: Ограничьте использование API ключа (рекомендуется)

1. В **Credentials** нажмите на созданный API ключ
2. В разделе **Application restrictions** выберите **HTTP referrers (web sites)**
3. Добавьте домены:
   - `https://www.padelo2.com/*`
   - `https://padelo2.com/*`
   - `http://localhost:3000/*` (для разработки)
4. В разделе **API restrictions** выберите **Restrict key**
5. Выберите только нужные API:
   - Maps Embed API
   - Maps JavaScript API (если используется)
6. Сохраните изменения

### Шаг 4: Добавьте ключ в Vercel

1. Откройте **Vercel Dashboard** → Ваш проект → **Settings** → **Environment Variables**
2. Нажмите **Add New**
3. Добавьте переменную:
   - **Key:** `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`
   - **Value:** ваш API ключ из Google Cloud Console
   - **Environments:** Production, Preview, Development
4. Сохраните изменения

### Шаг 5: Дождитесь деплоя

После добавления переменной Vercel автоматически перезапустит деплой (1-2 минуты).

## Альтернатива (без API ключа)

Если не хотите использовать Google Maps API, карта будет заменена на ссылку "Открыть в Google Maps", которая работает без API ключа.

## Проверка

После деплоя откройте страницу турнира с адресом. Карта должна отображаться без ошибок.

## Стоимость

- **Maps Embed API**: Бесплатно до 25,000 запросов в месяц
- **Maps JavaScript API**: Бесплатно до 28,000 загрузок карт в месяц

Для большинства сайтов этого более чем достаточно.

## Безопасность

⚠️ **Важно:** 
- Не коммитьте API ключ в Git
- Ограничьте использование ключа по доменам
- Ограничьте доступ только к нужным API
- Регулярно проверяйте использование в Google Cloud Console

