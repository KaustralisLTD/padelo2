# 🔧 Настройка переменных окружения в Vercel

## 📋 Шаг 1: Откройте настройки проекта

1. Зайдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект **PadelO2**
3. Перейдите в **Settings** → **Environment Variables**

## 📝 Шаг 2: Добавьте переменные для базы данных

Нажмите **"Add New"** и добавьте следующие переменные **для всех окружений** (Production, Preview, Development):

### Обязательные переменные:

1. **DATABASE_HOST**
   - **Value:** `foldis00.mysql.tools` (или ваш хост БД)
   - **Description:** Хост базы данных MySQL

2. **DATABASE_PORT**
   - **Value:** `3306`
   - **Description:** Порт базы данных (обычно 3306)

3. **DATABASE_NAME**
   - **Value:** `foldis00_padelo2` (или ваше имя БД)
   - **Description:** Имя базы данных

4. **DATABASE_USER**
   - **Value:** `foldis00_padelo2` (или ваш пользователь БД)
   - **Description:** Имя пользователя базы данных

5. **DATABASE_PASSWORD**
   - **Value:** `ваш_пароль_от_бд` (ваш реальный пароль)
   - **Description:** Пароль для подключения к БД
   - ⚠️ **Важно:** Не показывайте этот пароль никому!

### Опциональные переменные:

6. **DATABASE_SSL**
   - **Value:** `true` (если БД требует SSL) или `false`
   - **Description:** Использовать SSL для подключения

## 🔍 Где взять эти данные?

### Вариант 1: Из панели хостинга (cPanel/Plesk)

1. Зайдите в панель управления хостингом
2. Найдите раздел **"Базы данных MySQL"** или **"MySQL Databases"**
3. Там будут указаны:
   - **Хост БД** (Host)
   - **Имя БД** (Database name)
   - **Пользователь БД** (Username)
   - **Пароль БД** (Password) - если забыли, можно сбросить

### Вариант 2: Из локального файла .env.local

Если у вас уже настроена БД локально, откройте файл `.env.local` в корне проекта и скопируйте значения оттуда.

### Вариант 3: Свяжитесь с поддержкой хостинга

Если не знаете данные БД, обратитесь в поддержку хостинга и попросите:
- Хост MySQL сервера
- Имя базы данных
- Имя пользователя БД
- Пароль БД

## 📋 Шаг 3: Дополнительные переменные (опционально)

Если используете другие сервисы, добавьте:

### Для отправки email (Resend):

7. **RESEND_API_KEY**
   - **Value:** `ваш_api_ключ_resend`
   - **Description:** API ключ для отправки email через Resend

### Для сайта:

8. **NEXT_PUBLIC_SITE_URL**
   - **Value:** `https://www.padelo2.com`
   - **Description:** URL вашего сайта

9. **ADMIN_CREATE_SECRET** (опционально)
   - **Value:** `какой-то-случайный-секретный-ключ`
   - **Description:** Секретный ключ для защиты endpoint создания админа

## ✅ Шаг 4: Сохранение и деплой

1. После добавления всех переменных нажмите **"Save"**
2. Vercel автоматически перезапустит деплой
3. Подождите 1-2 минуты
4. Проверьте endpoint: `https://www.padelo2.com/api/admin/check-db`

## 🧪 Проверка

После добавления переменных откройте:

```
https://www.padelo2.com/api/admin/check-db
```

Должен вернуться JSON с:
```json
{
  "success": true,
  "database_configured": true,
  "database_connected": true,
  "admin_exists": true,
  "message": "Database connected. Admin exists."
}
```

## ⚠️ Важные замечания:

1. **Не добавляйте переменные в код!** Они должны быть только в Vercel Dashboard
2. **Используйте одинаковые значения** для Production, Preview и Development (или разные, если нужно)
3. **Пароль должен быть надежным** и не должен быть в Git
4. **После изменения переменных** Vercel автоматически перезапустит деплой

## 🆘 Если что-то не работает:

1. Проверьте, что все переменные добавлены для **Production** окружения
2. Убедитесь, что значения скопированы правильно (без пробелов)
3. Проверьте логи деплоя в Vercel Dashboard → Deployments → Logs
4. Откройте `/api/admin/check-db` и посмотрите, какая ошибка

