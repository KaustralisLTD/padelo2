# ⚡ Быстрый деплой на Vercel

## ✅ Vercel CLI установлен!

Теперь у вас есть 2 варианта:

---

## 🚀 Вариант 1: Прямой деплой через CLI (САМЫЙ БЫСТРЫЙ)

### Выполните команду:

```bash
cd "/Users/spilberg/AI Projects/PadelO2"
vercel
```

### Следуйте инструкциям:

1. **Set up and deploy?** → Нажмите `Y` (Yes)
2. **Which scope?** → Выберите ваш аккаунт
3. **Link to existing project?** → Нажмите `N` (No - создать новый)
4. **What's your project's name?** → Введите `padelo2` или любое имя
5. **In which directory is your code located?** → Нажмите `.` (Enter - текущая папка)
6. **Want to override the settings?** → Нажмите `N` (No)

### Готово! 🎉

Vercel автоматически:
- Определит Next.js
- Задеплоит проект
- Дам вам ссылку типа `https://padelo2.vercel.app`

---

## 📦 Вариант 2: Через GitHub (для автоматических обновлений)

Если хотите, чтобы при каждом `git push` был автоматический деплой:

### Шаг 1: Инициализируйте Git

```bash
cd "/Users/spilberg/AI Projects/PadelO2"
git init
git add .
git commit -m "Initial commit"
```

### Шаг 2: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Название: `padelo2`
4. **НЕ** добавляйте README, .gitignore, лицензию
5. Нажмите **"Create repository"**

### Шаг 3: Загрузите код

```bash
git remote add origin https://github.com/ВАШ_USERNAME/padelo2.git
git branch -M main
git push -u origin main
```

(Замените `ВАШ_USERNAME` на ваш GitHub username)

### Шаг 4: В Vercel Dashboard

1. Зайдите в [vercel.com/dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New Project"**
3. Выберите ваш репозиторий `padelo2`
4. Нажмите **"Import"**
5. Vercel автоматически определит настройки
6. Нажмите **"Deploy"**

---

## 🎯 Рекомендация:

**Начните с Варианта 1** - это займет 2 минуты!

Потом, если нужно, можно подключить Git для автоматических обновлений.

## ✅ После деплоя:

- Сайт будет доступен по ссылке типа `https://padelo2.vercel.app`
- Можно настроить свой домен в настройках проекта
- Все языки будут работать автоматически

